{
  "name": "ai-gaming-pet-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "node --experimental-strip-types src/llm-status.test.ts",
    "test:rust": "cargo test --locked --manifest-path src-tauri/Cargo.toml",
    "lint:rust": "cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "format": "prettier --write \"src/**/*.ts\" index.html package.json package-lock.json tsconfig.json vite.config.ts src-tauri/tauri.conf.json \"src-tauri/capabilities/**/*.json\"",
    "format:check": "prettier --check \"src/**/*.ts\" index.html package.json package-lock.json tsconfig.json vite.config.ts src-tauri/tauri.conf.json \"src-tauri/capabilities/**/*.json\"",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vite": "8.2.1"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  }
}
