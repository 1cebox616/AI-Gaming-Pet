# AGENTS.md
最后更新：OCR 生产定型写入 D-47（v6 tiny / 2 线程 / det 1280 / 内存直传，心跳帧也读）；B-T4b 规格已出；新增 TD-36/37

> 本文件是主干、端口、跨游戏共识、流程与工程原则的唯一来源。CS2 适配的契约与实测见 **CS2.md**；
> 新游戏适配开工见 **ADAPTER_GUIDE.md**。每份事实只存在于一个文件中，其他文件引用不复制。
> 三份 md coding agent 一律不得修改（例外条件见 §3.7）。架构师每轮验收第一个动作是拉仓库逐字 diff。

---

## 0 先读这一页

**开工顺序**：§0 铁律 → §6.2 当前任务 → 涉及的 §2 契约 → §3 相关规则 → §4 失误模式。
改 `games/cs2/` 前读 CS2.md。规则条目带编号（D-xx 决策、F-xx 失误模式、TD-xx 技术债），提交信息与报告可直接引用。

**铁律（违者打回）**

1. AGENTS.md / CS2.md / ADAPTER_GUIDE.md 不得修改，只在提交中带上。
2. 分层：core 不 import games；游戏包互不 import；生产模块不 import 本游戏 eval/；pet/ 顶层只有 main.py（及 __init__.py）；
   游戏包只能 import core 的 adapter_api / config / llm / prompt（games/generic 另加 capture / input_telemetry / belief；
   eval/ 另加 gate）。`tests/test_layering.py` 静态强制，违者指出文件与行号。
3. 隐私：截屏、录制、输入遥测、belief/、指纹、游戏卡不得 import 任何网络模块；产物只写入 .gitignore 覆盖的目录；
   入库产物不得含真实玩家身份；发给模型的永远是抽象信号，不是键码流。
4. 密钥只从环境变量读（默认 OPENROUTER_API_KEY），永不进配置文件或源码；模型型号与上游一律配置传入。
5. 禁止桩代码、写死返回值、mock 混入生产路径、空实现、TODO、裸 except 静默吞异常。做不到就如实说明。
6. 每任务 commit 并 push；提交信息以任务 ID 开头；`git ls-remote` 确认远端存在后再报告。
7. 任何阈值必须附实测依据，或明写"此数待实测确定"。
8. 「已实现」「未实现」「技术债」是覆盖语义不是追加（§3.1 原则 12）。

**术语**

| 词 | 含义 |
|---|---|
| 主干 / 适配 / 端口 | 主干管嘴、钱包、安检、脸；每个游戏的理解管线是一个适配；两者只经端口 v1 通信 |
| 快线 / 深线 | 通用视觉的两条模型调用：快线每帧一句散文、≤1 秒、发言链路在等；深线按需深读、不限长度、无人等 |
| 证据流 / 归并器 / 信念状态 | 一切观察写成只追加的证据；唯一写者（代码）按策略裁决成信念；散文是渲染视图 |
| 笔记员 | 文本模型，每几分钟读证据提补丁（升格 / 驳回 / 翻案 / 合并 / 提问），归并器验证后执行 |
| 游戏卡 | 每游戏一份跨会话持久记忆：场景图谱、HUD 槽位、键位、货架一事实。在线渐进生成，零预置 |
| 场景指纹 | 画面缩成 64 位感知哈希，只答"这画面见过没有"，微秒级、无模型无 GPU |
| 检测器 | 块级自适应噪声地板的画面变化检测；职责是去重不是侦察 |
| 两货架 | 游戏知识边界：货架一"玩前可得"（可查）、货架二"玩后所见"（只能观察） |

**现在**：M5-B「跟得上」；B-T1 已通过；OCR 引擎已定型；**B-T4b 执行中**（最简单遍式接管线 + 游戏中实测）；T7.8（429 冷却）并行。详见 §6.2。

---

## 1 项目

### 1.1 目标与形状

一个常驻 Windows 桌面的 AI 电子宠物，在用户玩游戏时实时观战、解说、吐槽。
主干与游戏解耦：每个游戏的完整理解管线（数据接入 → 事件 → 发言决策 → 事实句）打包成一个独立适配，
经统一端口向主干递「发言请求」。主干只负责嘴（语音与开关）、钱包（模型调用与花费）、安检（闸门引擎）、脸（前端桥）。
已落地适配：CS2。战争雷霆适配由第二位开发者依 ADAPTER_GUIDE.md 开发中。
通用视觉模式（M5）架构上是主干侧能力：相当于一个"用屏幕当数据源"的特殊适配，经既有端口产出发言请求。

### 1.2 技术栈

| 层 | 选型 |
|---|---|
| 后端 | Python 3.12.x（>=3.12,<3.13）+ FastAPI 0.141.1 + uvicorn 0.52.1 + pydantic 2.13.4 + websockets 17.0.1 + httpx 0.28.1。仅绑定 127.0.0.1:8737 |
| 前端 | Tauri 2.11.5 + TypeScript 7.0.2 + Vite 8.2.1 + Prettier 3.9.6，不使用 UI 框架 |
| 构建 | Node.js 24.x、npm 11.x、Rust 1.97.x（MSVC） |
| 语音 | Windows OneCore 系统语音：PowerShell 合成 WAV + WinMM 直播放，可从任意线程立即中断，零模型零 GPU。神经网络语音是 M7 |
| 语言 / 视觉模型 | 经 OpenRouter（OpenAI 兼容协议）。产品负责人在北美，无法注册阿里云百炼，不走厂商直连。型号与上游一律配置传入 |
| 截屏 | Windows Graphics Capture（zbl 0.7.1），按精确 HWND 抓单窗口 |
| OCR | WinRT Windows.Media.Ocr（zh-Hans-CN 可用），原型阶段 |
| 打包 | PyInstaller + Tauri bundler（尚未启用，bundle.active = false） |

### 1.3 目录结构

```
/backend/src/pet/
    main.py                  读配置、装载当前适配、挂路由、生命周期
    core/                    主干，游戏无关（分层由 tests/test_layering.py 强制）
        adapter_api.py       端口 v1：SpeechRequest / GameStatus / GameAdapter / CoreServices（§2.1）
        speaker.py           发言执行：串行队列、模型调用、回退、花费、模式锁定
        gate.py              闸门引擎：按 vocabulary_id 解析词表绑定表为运行时闸门
        capture.py           截屏与画面变化检测（M5-T1/T3）：WGC 抓帧、块级检测器、全量录制、
                             离线重放、前台命令行探针。零模型、零网络
        capture_calibration.py / capture_camera_diagnostic.py   检测器校准与转向诊断（离线工具）
        input_telemetry.py   键鼠遥测（Raw Input）：生产用动作键白名单监听器与探针全键盘模式
                             在代码路径上分离（D-17）
        ocr_probe.py / ocr_selective.py / ocr_selective_probe.py
                             WinRT OCR 探针与选择性二次 OCR 原型（持久工作进程、文字行缓存差分、
                             预算与 scene_reset 爬坡）。尚未接管线（B-T4）
        belief/              【B-T1 起新建】证据流、主张、归并器、信念状态、渲染器（§2.6）。
                             零网络零模型：笔记员与深线的模型调用在适配侧发起，belief 只验证与执行补丁
        config.py  network.py  bridge.py  speech.py  lines.py  llm.py  prompt.py
    games/<game_id>/         每游戏一个适配包，含完整管线与自己的 eval/（CS2 模块清单见 CS2.md）
    games/generic/           通用视觉模式（默认关闭 [games.generic].enabled）
        adapter.py           快线接管线（M5-T7 系列）：窗口标题查表识游戏、帧选取、单图上传、
                             并发与超时丢帧、输入上下文、观察日志落盘、状态发布
        eval/                视觉考卷（vision_exam / vision_endpoint_exam / vision_geo_exam）、
                             机械区域生成（region_assets）、离线重放与矩阵（observation_replay / observation_matrix）
        【B-T2 起新增】场景指纹、游戏卡、初始化、笔记员调用等模块，文件名以各任务规格为准
/backend/tests               后端测试与 fixtures；test_layering.py 是分层警报器
/backend/prompts/
    personality.md           共用性格层，产品负责人可直接编辑，重启生效
    generic/                 通用视觉提示词：observation-fast.md（快线）与 observation-deep.md（深线）；
                             B-T3 / B-T7 新增初始化与笔记员提示词。跨游戏通用，不得含任何游戏专属内容，
                             游戏信息只以数据（游戏卡）身份进上下文
    <game_id>/               每游戏的词库、闸门需求（gate-requirements.json）等
/backend/data/<game_id>/     每游戏的场景与评测资产（回归资产，不得删除）
/backend/eval-reports/       评测输出（已 gitignore）
/backend/recordings/         原始录制与观察日志会话目录（已 gitignore）
/backend/memory/<game_id>/   【B-T2 起新建，须加入 .gitignore】游戏卡（gamecard.json + gamecard.md）与
                             会话信念快照。跨会话持久的用户数据，不是回归资产
/backend/audit/              M4-a 的模块清单（历史参考）
/backend/config.toml         默认配置；config.local.toml 本地覆盖（已忽略）
/frontend                    Vite + TypeScript 前端；/frontend/src-tauri Tauri 外壳
/docs/<game_id>/             每游戏的文档（数据接口能力、社群黑话等）
```

### 1.4 命令与约定（Windows PowerShell）

- 后端安装：`cd backend && python -m venv .venv && .venv\Scripts\pip install -r requirements.txt`
- 后端运行：`.venv\Scripts\python -m pet.main`　　后端测试：`.venv\Scripts\python -m pytest`
- CS2 评测：`python -m pet.games.cs2.eval.<replay|bench|scenario_synth|...>`（用途见 CS2.md）
- 通用视觉：`python -m pet.games.generic.eval.<observation_replay|observation_matrix|vision_exam|vision_endpoint_exam|vision_geo_exam>`；
  离线初始化与信念重放入口随 B-T3 / B-T6 建
- 前端：`cd frontend && npm.cmd install` / `npm.cmd run tauri dev`（需先启动后端）/ `npm.cmd run build` 与 `format:check`
- 前端命令一律 `npm.cmd`；python 不可直呼时用 `py -3.12`；后端与前端由人手动分别启动，打包（M10）前不做自动拉起

---

## 2 契约（改动前必须先与架构师确认；以下定义与代码逐字段一致）

### 2.1 适配端口 v1（core/adapter_api.py，PORT_VERSION = 1）

```
SpeechRequest —— 适配向主干递的唯一「发言条子」
    request_id, game_id, fact_text（事实句）, urgency（0–100）,
    interrupt, supersedes_request_id（二者本版只入库不实现差异化行为，当前等价于一律 interrupt=True；
    语义留给需要的适配提出后实现）,
    vocabulary_id（闸门与提示词用哪份游戏词表，None=不加）, llm_profile（模型档位名，None=默认 [llm]）,
    fallback_text / fallback_emotion（模型失败时的模板兜底）, ts
GameStatus —— 适配向主干报的游戏状态
    game_id, state（适配自定粗粒度状态）, summary: dict（展示字段，主干不解释语义，前端与托盘泛化显示）
GameAdapter（Protocol）
    adapter_id, display_name, port_version（装载时必须等于 PORT_VERSION，不等则拒绝装载并记 ERROR）,
    http_router | None, async start(core), async stop()
CoreServices —— 适配对主干的全部认知，仅此五个回调
    submit_speech(SpeechRequest), publish_status(GameStatus),
    can_submit_speech() -> bool（是否有前端消费者，供适配决定是否扣配额）,
    speech_is_muted() -> bool, reset_speech_session()（对局边界重置连续失败锁定）
```

端口带版本号；不匹配拒绝装载。端口不够用时**立即停下来找架构师改端口**，不许在适配里绕路——开发者只有两人，端口还能免费改。

### 2.2 主干对发言条子的三项裁决与 speaker 行为

裁决（宠物的生理，不是游戏逻辑，永远留在主干）：
1. 开关永远有效——静音时任何适配的条子都不播
2. 只有一张嘴——同一时刻只播一句，打断规则统一执行
3. 频率保险丝——粗粒度全局每分钟上限，防写坏的适配把宠物变成机关枪【既定设计，实现排在战雷接入前，TD-3】

speaker.py（自 M3 起不变）：请求串行队列先进先出；一次事件最多一次模型调用绝不重试；
失败 / 空输出 / 闸门命中一律回退 fallback_text；连续失败 3 次锁定本局模板模式；花费统计与托盘显示。

### 2.3 提示词拼装与闸门引擎

- core/prompt.py：系统提示 = prompts/personality.md + prompts/<vocabulary_id>/vocabulary.md（若有）。
  性格层共用、游戏知识层随适配（D-04）。词库整份注入不做筛选（输入几乎免费，§5.5）。
- core/gate.py：按 vocabulary_id 读取 prompts/<id>/ 下的词表绑定表与 gate-requirements.json，解析为运行时闸门；
  解析失败整表失效并回退保守规则。改词库不需要改代码。

### 2.4 前端桥

WebSocket `ws://127.0.0.1:8737/ws`：
```
后端 → 前端  {"type":"utterance","id":...,"text":...,"emotion":...}
            {"type":"state","speech_enabled":bool,"muted":bool,"game":{"game_id":...,"state":...,"summary":{...}}}
前端 → 后端  {"type":"request_idle_line"}  {"type":"set_speech_enabled","value":bool}  {"type":"set_muted","value":bool}
```
规则：连接建立先发 state 再发问候；开关变化广播 state；game 字段任何时候必须存在，未知子字段为 null；
非法消息只记 WARNING 不断连；Origin 存在但不在白名单则拒绝，缺失则允许。

Utterance（core/lines.py）：id 非空, text 非空, emotion ∈ neutral / happy / angry / surprised / speechless。
HTTP 路由：适配经 http_router 挂载自己的接收端点（CS2 的 /gsi 规则见 CS2.md）。

### 2.5 配置文件（backend/config.toml）

```
[active]  game = "cs2"
[speech] [idle] [llm]            顶层，主干所有
[llm.profiles.<name>]            模型档位覆盖段。可选字段：base_url（该档位专用端点，缺省用全局）、
                                 api_key_env（密钥所在环境变量名，缺省 OPENROUTER_API_KEY）
[games.<game_id>.*]              各游戏自己的配置段（CS2 见 CS2.md）
[games.generic]                  当前生产值：poll_interval_seconds=1.0, send_width=896, fast_timeout_seconds=5.0,
                                 max_inflight=4, max_tokens=200（保险丝）, region_focus_max=0.50
                                 （吸收原 region_sparsity_max=0.25，TD-18 待扫统一）
【B-T2 起新增】[games.generic.belief]（笔记员周期、策略参数）、[games.generic.init]（初始化预算与代表帧数）、
              [llm.profiles.vision_deep]（最强档，无延迟约束）、[llm.profiles.notetaker]（文本档）
```
密钥值永远不进配置文件；指定的环境变量缺失时必须明确报错，不得回退到其他变量或默认端点。
仅默认端点发送服务商特有的路由参数。

### 2.6 信念层契约 v1（core/belief/，B-T1 起分任务落地）

数据模型属难回退决策（§3.1 原则 7）。

```
EvidenceEvent —— 证词。只追加、不修改、错了也保留
    evidence_id, source（fast / deep / ocr / input / mouse / scene / detector / init / gsi）,
    kind（随来源定义：fast_observation / deep_observation / text_observed / key_window / mouse_motion /
    scene_fingerprint / frame_metrics / init_fact …）, root_capture_id（源自哪一帧的 seq；非帧来源 None）,
    observed_at（内容时刻，会话内 T+秒）, learned_at（系统得知时刻）,
    scope（归一化 bbox 或方位词，无则 None）,
    payload（各 kind 自定 pydantic 模型；OCR 的数字 / 文字 / 位置 / streak 保持原样，不得改写成句子）,
    derived_from（派生自哪些 evidence_id）, context_version（模型看见的是哪一版认知；非模型来源 None）,
    outcome（ok / dropped / superseded / failed）
Claim —— 标准化后可裁决的最小主张
    subject（entity:eN / ui:rN / scene / player / game）,
    predicate（通用层只有十个：visible / located_at / numeric_value / reads_text / same_as / has_label /
    has_state / changed_from / doing / contains；游戏适配可加命名空间谓词，通用层不预定义任何游戏机制）,
    value, value_type, valid_from, evidence_ids, modality（observed / inferred / uncertain；不接受模型自报数值置信度）
BeliefSlot —— 每个 (subject, predicate) 一格，格里是候选而非单值
    key, current, status（candidate / confirmed / superseded / retracted；disputed 与 stale 由字段计算不落库）,
    valid_from, last_observed_at, support_groups（按 root_capture_id 家族分组，同帧派生只算一组）,
    contradictions, policy（ui_numeric / ui_text / scene / default）, history
StatePatch —— 笔记员与深线对语义层的提议，不是写入
    ops[]：set_candidate / upgrade / retract / merge_entity / open_question，每条必须引用 evidence_ids 与 valid_at
BeliefSnapshot —— version, generated_at, slots, entities（entity_id, aliases, current_label, identity_status, last_seen）,
    open_questions, conflicts
ActionLog —— 每次升格 / 驳回 / 翻案 / 替代 / 合并 / 拒绝补丁一行，引用证据与规则名
BeliefReducer —— 唯一写者、纯函数：(snapshot, evidence | patch) -> (snapshot', actions)
    验证：证据存在、时间合理、迁移合法、满足升格规则、不重复计数、推测与"仅光效"不成为事实、不删除未解释的矛盾
视图 —— 皆由代码从 BeliefSnapshot 渲染，可丢弃可重生，不是数据库
    cognition.md（人读）、快线认知短视图（分钟级，长度硬顶）、深线待查队列、发言候选（M5-C）、调试证据图
```
已落地部分（B-T1）：EvidenceEvent 三种 kind（fast_observation / frame_metrics / key_window）、EvidenceStore、
observations.md 渲染器。evidence.jsonl 按 learned 顺序追加，md 按帧序渲染。fast_observation.latency_ms 对丢弃记录
是"调度到判定"的耗时（此前恒 0）。当前校验器是封闭世界（kind 白名单、root_capture_id 必须非空、同帧同 kind 唯一），
B-T4（OCR 一帧多条）与 B-T5（鼠标无帧号）必须同批放宽。

### 2.7 游戏卡契约 v1（backend/memory/<game_id>/gamecard.json + gamecard.md，B-T2 起）

```
game_id, display_name（来自窗口标题查表，只管身份）,
genre, perspective（first / third / 2d / card …；来自运行时货架一查询，source=init，可空）,
scenes[]（cluster_id, label, representative_hash, first_seen, seen_count, evidence_ids）,
hud_slots[]（ui:rN, bbox, semantic_role, role_status, evidence_ids）,
keybinds{}（键 → 含义, source: default_table / observed, support_count）,
view_constants（yaw_deg_per_count 若公开；user_sensitivity 由玩家提供，可空）,
init（initialized_at, source_recordings, version）
```
注入快线的短视图由渲染器生成，长度硬顶（此数待实测；每 1000 token = +1 秒）。跨会话持久，是 M6 的种子。

---

## 3 规则

### 3.1 工程原则（对架构师与 coding agent 同等适用）

1. 不留向后兼容：过时代码直接删。（例外：已有真实数据的存储结构变更须先确认；模板回退路径是产品决策不是兼容层，必须保留。）
2. 选满足当前需求的最简实现。不做预防性抽象。
3. 先跑通最小端到端版本再往上加。绝不为未完成的功能拆掉能跑的东西。
4. 关注点分离，按已存在的职责边界拆，不为设想拆。
5. 优先用成熟且在维护的库。
6. 加依赖或自己写之前，先查现有依赖能否解决并说明查过什么。
7. 难以回退的决策（数据模型、核心接口、技术栈）按长期方案定；**适配端口与信念层数据模型属于此类**。易替换的实现按原则 2 从简。
8. 先看成熟产品如何解决同类问题，按本项目规模裁剪。
9. 底层行为不把假设写成诊断：按 a→b→c 尝试、每步测量、够用就停。
10. 修好一处规则后，主动搜索还有哪些地方适用同一条。
11. "能做什么"的清单优先用程序从真实数据生成，不由人或 agent 撰写。
12. 「已实现」「未实现」「技术债」是覆盖语义不是追加。自查方法：只比对两个清单中以 `- ` 或数字编号开头的条目行的
    任务 / 里程碑 ID 是否有交集；不要在全文范围统计 ID 出现次数（交叉引用不构成重复）。

### 3.2 跨游戏决策（仍在生效，不要重新讨论）

- **D-01 确定性的事实用代码算，需要品味的表达用大模型生成。** 事件判定、局势累计、事实句全部由代码确定性产出；
  模型只负责"把事实句用网友口气重说一遍"。所有适配同一形状。
- **D-02 代码写平实事实，不写社群黑话，不使用内部档位名。** 让代码写黑话等于把品味判断收回代码；写档位名等于造一层需要模型解码的编码。
- **D-03 发言策略属于适配，不属于主干。** CS2 事件间隔 2–3 秒、战雷导弹飞行十几秒，任何"通用策略"都是把一个游戏的实测当所有游戏的规律。
  CS2 磨出来的策略机制作为可选工具箱保留，不强加。
- **D-04 性格与游戏知识分层。** 性格层（怎么说话）共用一份、产品负责人直接编辑；游戏梗知识随适配走；拼装由主干完成。
- **D-05 模型的事实准确率不是评测指标。** 事实由代码保证；要检验的是文风好不好听，只能由人肉眼判断。
  三件事分开：事实准不准（代码 + 单元测试）、会不会漏说（人耳）、会不会编造（运行时闸门）。
- **D-06 模板路径永久保留**，模型失败时回退，宠物永不哑火。不设花费熔断，只做显示。
- **D-07 回合 / 段落结束不发言**，槽位留给视觉层之后的长记忆反馈。
- **D-08 不做交火期禁言**：及时的情绪反馈本身就是卖点。
- **D-09 游戏身份由窗口标题与进程名确定性获得，不是模型的推理任务。** 准确率 100%、成本 0、延迟 0。让模型去猜是设计倒退。
  查表**只管身份**，不扩知识字段；不在表内的游戏以标题原文作为名称（D-37）。

### 3.3 通用视觉 · 输入侧（截屏、检测器、输入遥测）

- **D-10 时间线的更新频率靠并发，不靠单次变快。** 单次往返已接近管道物理下限（§5.3）；每秒发一帧、允许数个请求在飞、按时间戳入库。
  串行队列是"嘴"的规矩，不是"眼睛"的。
- **D-11 通用检测器的职责是去重，不是侦察。** "准心里的远处小目标"在块平均度量下物理上不可分（5% 像素 × 大变化被稀释成 5/255），
  可靠检测此类目标是游戏特化视觉适配的职责。缓解：固定基线的累积效应（晚发现而非发现不了）、60 秒心跳、中心加权 margin（TD-24）。
- **D-12 帧选取不做时间持久性门控（P=1 定案）。** 1 秒采样下真实事件多数只在一帧里表现为剧变，"连续两轮才算数"把它们成批滤掉
  （§5.2 P=2 数据）。滤闪烁由块级自适应噪声地板独立承担。**门控的时间尺度必须与目标事件的时间尺度比对后再设。**
- **D-13 camera_motion 规则已删除。** "镜头移动"在像素空间不是可分类别（转向 / 非转向块占比分布严重重叠：中位 13.2% vs 4.9%，
  P75 22.6% vs 19.3%，非转向最大 97.9%）。其作用由 P=1 下的 persistent_change 上传与稀疏抑制承担。
- **D-14 上传与区域提示是两个独立决策，不得重新耦合。** 是否上传只看有无 confirmed 块；是否附区域提示只看 confirmed 占比是否
  ≤ region_focus_max。超过上限时清空提示但照常上传。
- **D-15 检测器参数选型用"带约束筛选"，不用字典序排序。** 纯"漏帧优先"会一路推到 k=0.8/margin=0 的"全都传"角落（纯静止画面也传 62%，等于检测器关闭）。
  硬约束（字幕 / UI 剧变零漏检、过渡段漏检个位数、世界事件命中不垫底）之内最小化真阴性锚点（B-idle 与 A-static）的上传率；
  燕云类"零输入动态世界"只作成本列。生产定值 N=10 / k=1.2 / margin=4/255 / P=1（依据 §5.2）。
- **D-16 两条时间线（画面 / 输入）用同一个单调时钟；帧时间尽量取内容时刻。** 键鼠几乎零延迟，画面天生滞后，实测错位约一轮。
  修法三层：统一 perf_counter、帧时间戳取 WGC 呈现时刻（若 zbl 暴露）、互相关校准兜底（TD-22）。
  发给模型的场景对精度宽容，校准真值才吃精度。
- **D-17 输入遥测隐私两档白名单（实现见 TD-23）。** 动作键（通用基础键 + 当前游戏键位表声明的键，逐事件记录）；
  其余可打印键只记窗口内计数，不记身份与顺序——无身份无顺序的计数在数学上无法重建文本。另加打字风暴静默与输入法合成静默。
  **开发阶段临时开启全键盘记录是产品负责人的显式决定**：仅限本机、仅进被忽略目录，提交进仓库的任何报告不得含动作键集合之外的逐键数据。
- **D-18 键位输入进快线：机械事实，不做语义翻译。** 格式如"W 按住 2.8 秒；左键点击 5 次（间隔约 0.15 秒）；鼠标向右大幅转动"；
  "W=前进"由模型结合游戏名自解（键位表属游戏卡）。生产适配自带动作键白名单监听器，与探针全键盘模式在代码路径上分离。
- **D-19 像素不是运动传感器，鼠标才是。** 转没转视角由鼠标位移直接得知，聚合成"方向 + 幅度"的结构化证据，用来把画面变化归因到
  玩家自己的动作（打击"出现了敌人"类误报）。绝对角度只在游戏卡有公开的每计数角度常数、玩家提供了灵敏度、且为普通视角时给，
  否则只给相对量。光流自校准不做。

### 3.4 通用视觉 · 快线与深线

- **D-20 快慢两条线，成本性质相反。** 输入几乎免费、输出串行昂贵，同一次请求无法兼得"快"与"深"。
  快线（热路径）：每个上传帧都跑，发言链路在等，预算 ≤1 秒，一句话硬顶，不联网，产出进证据流（source=fast）。
  深线（冷路径）：只挑少数帧、被触发才跑，无人等待，不限长度，可联网（受 D-40 约束），产出进证据流（source=deep）。
  两者以"证词"身份进同一条证据流：机械事实由归并器裁决，语义由笔记员提补丁、归并器验证。
  触发深线只有三种：归并器待查（矛盾槽位、未标注场景簇、实体歧义）、笔记员提问、闲时补课；快线举手用现成信号
  （观察含"读不清 / 不确定"），提示词不为此改动。**再多的深度需求都表达为"待查问题"，不加第三条线。**
- **D-21 快线上下文永远是单图，且只放分钟级稳定或机械生成的信息。** 判据：并发使一切秒级更新的上下文必然过期。
  清单：当前帧 + 游戏名 + 区域提示（含真实基线秒差）+ 键鼠输入窗口（机械聚合）+ 游戏卡短视图（稳定前缀首位）+
  认知短视图（分钟级，cover 语义，附 context_version；B-T8 接入）。文字帧时间线已移除——秒级信息并发下必然过期，
  且实测为复读抄袭的病灶。不做多帧并送。提示词组装"稳定前缀在前、变化部分在后"以吃前缀缓存。
- **D-22 快线输出合同（T7.9 版，冻结）：模型只写散文，数字全部机械注入。** 输出【全局画面】（一到两句）+【局部】
  （仅当提供聚焦区域时）。允许复读——重复即"场景稳定"的证据，去重归后续层。回溯禁令：现在进行时合法，"出现了 / 比之前"非法。
  日志渲染器机械注入三指标（全局像素变化、区域占比、区域强度）与【玩家输入】行；模型一个数都不写，无从编造。
  聚焦区域以外接框方位词 + 占比表述（"画面中央偏左、约 30% 的区域"），**格子编号不发送给模型**；区域占比 >0.5 不提供聚焦（"占比高=镜头动"已被否证）；
  心跳帧注明定时快照。字数为软引导，max_tokens=200 只作保险丝。md 正文时间用 T+秒，头部留墙钟锚点。
- **D-23 事实优先级阶梯（T7.5 实测有效）。** 可读文字与数字 > 对象出现 / 消失 / 移动 > 界面结构 > 纯光效（须以"仅"开头四到六字自标记）。
  杀戮尖塔含数字占比 6.4%→34.3% 验证方向；统计口径须排除系统注入符号（坐标假数字曾把灰区"含数字"污染到 26.7%）。
- **D-24 快线不做深思，外部机制替它卸负。** 区域框、三指标、优先级阶梯、键位摘要、游戏卡——外部机制每强一分，模型需要的内部推理就少一分。
  快线不做推理已定案：现任模型推理为开关无档位，开启延迟不可接受（T7.11）。轻推理若重开只作单变量 A/B，M5-C 前重新评估。
  **常设偏好：能在本地用程序算出来的，绝不交给线上模型。** 检测器、OCR、场景指纹、鼠标聚合、GSI、帧环形缓冲都是这条的产物；
  每加一个本地传感器，就重新审视快线提示词里还有哪一段可以删。
- **D-25 拒绝有状态记忆模型方案。** 与并发数学不相容（有状态要求按序）；丢帧即失同步且模型不自知；图像 token 缓存支持参差不可赌；
  违反 D-36。提示词缓存作为正交小优化保留。
- **D-43 态势感知层延迟预算：MVP 端到端 ≤ 3–4 秒。** 从帧被选中到宠物开口，反射路径 = 检测器（毫秒）+ 快线 + 归并器（毫秒）+
  发言模型 + 语音出声（0.4 秒）。分配初值：快线 P50 ≤ 2.0 秒、发言模型 ≤ 1.0 秒，合计约 3.5 秒；分配待实测修订。
  **现阶段允许超出**，但每个碰快线的任务必须报告延迟增量，并对照 §5.3 的账。快线输出改结构化的探针（B-T1.5）
  必须把输出解码速率补进账本。
- **D-44 传感器先行，快线只做残差；快线合同的重设计排在所有本地/前置传感器落地之后。**
  快线的职责 = 全部所需信息 − 本地传感器能确定性给出的部分。已确定属于传感器而非快线的：
  画面何时值得看（检测器）、屏幕上写着什么字与在哪（OCR + 游戏卡的槽位归属）、玩家按了什么转了多少（键鼠遥测）、
  这是哪个界面（场景指纹 + 游戏卡）、这个游戏怎么玩（游戏卡）、看不清时凑近看（深线）。
  减完之后快线只剩**开放世界语义**：画面里有什么对象、它是什么、正在发生什么。
  因此在传感器齐备前设计快线输出格式，等于为一份即将作废的职责清单做优化——B-T1.5 因此撤回并入 B-T8。
  重设计时同时评估三件事，不只是格式：**说什么**（残差职责）、**多长一句**（token 与 D-43 预算）、
  **多久一次**（传感器已能独立支撑的事件类型，可不等快线直接进证据流甚至直接发言）。
- **D-45 OCR 是常驻传感器；判据是"跟得上节奏 + 不跟游戏抢资源 + 召回最高"，不是一个抽象毫秒数（4a 后修订）。**
  原判据 P50 ≤100ms / P90 ≤250ms 作废：它是从 D-43 的反射路径预算切出来的，而 OCR 本地、异步、不阻塞帧派发，
  不在反射路径上。**衡量错了东西，所以换判据，不是因为没达标而放宽**——新判据要求一项此前从未测过的数据。
  三条同时成立才算达标：
  1. **跟得上**：P90 < 500ms（1 Hz 轮询下不积压）
  2. **不抢资源**：平均 CPU 核数 ≤ 0.5 核、**稳态** RSS ≤ 600 MB；**且游戏运行中实测帧时间与 1% low
     的劣化不可感知**（受 D-39 约束）。CPU 用**每次 OCR 的进程 CPU 核心秒数**度量（调用前后 cpu_time 差值），
     1 Hz 下这个数就等于平均占用核数——精确、可复现，不用任务管理器的百分比估算。
     **峰值并发核数与峰值 RSS 只记账不作门**（4a-2 修订）：限到 1 线程仍测出 2.0 核峰值，自相矛盾——
     要么线程限制没覆盖全部阶段，要么该比值被取样开销放大（收尾取样计入核心秒却未计入墙钟）。
     用一个自相矛盾的数当门是拿噪声做决策；真正的门是**游戏中的帧时间**，在 4b 测
  3. 在 1、2 之内**召回最高**。召回（找到的行 ÷ 画面上实际存在的行）比精确率优先——漏读的字永远不会进信念层，
     错读的字还能被 ui_text 策略按跨帧一致性驳回
  **新判据下多出来的延迟余量，第一用途是买 CPU**：推理线程数是最直接的杠杆，OpenVINO 默认吃满所有核，
  限制线程数用延迟换占用，对一个后台传感器是划算的交易。OCR 进程另设低于普通的优先级，让游戏永远赢。
  **OCR 跑通过检测器的帧与心跳帧**，不跑判为 no_change 的普通帧——那些帧文字不会变，差分按 stable 顺延（约省一半）；
  心跳帧（`reason="forced"`）保留一次全帧识别，用来兜住检测器漏检导致的长期失明（D-11 已知盲区，产品负责人定案）。
  **线程上限必须覆盖推理与预处理两侧**：4a-2 限到 1 个推理线程仍测出 2.0 核峰值，1 个线程用不出 2 个核，
  差额来自 OpenCV / OMP 各自的线程池（TD-35）。
  选型按 a→b→c 实测，不靠读文档推断（F-12）。允许用 GPU，但受 D-39 硬预算约束，且必须先证明 CPU 档在限线程下也不达标；
  为省几十毫秒去和游戏抢显存是赔本买卖，GPU 排在所有 CPU 手段之后。
  **先最简、后加机制（原则 2/3 的具体化）**：选定引擎后先跑全帧单遍式，用真实数据决定还需不需要 crop、预算、爬坡这些成本机制。
  T7.10b 的选择性机制是为 WinRT 的成本曲线设计的；若新引擎在全帧下就达标，这些代码**直接删**，不留"以后可能用得上"的配置层。
  已投入的原型工作不构成保留理由。**唯一无条件保留的是行差分**——它产出 change/streak，是证据语义不是成本优化。
- **D-46 图像格式分三条路，互不牵连（4a 实测）。** 生产里根本不存在"OCR 读文件"这条路——WGC 交出来的就是内存位图，
  为了 OCR 先编码成 JPEG 再解码是凭空制造工作。探针里的"文件输入"只是探针从磁盘读素材的产物，不是可选方案。
  本地 OCR **直接吃 WGC 内存位图**，绝不为了 OCR 先编码再解码
  （实测 JPEG 解码 P50 22ms、PNG 75ms，纯属白付）；OCR 回归资产**存无损 PNG**（压缩会改变小字边缘，实测同一帧
  PNG 出 26 行、JPEG 出 1 行，且各自内部稳定，是确定性差异不是抖动）；上云的 896 图**仍用 JPEG**，那是网络问题，与本地解耦。
  推论：**896 会毁掉小字**（候选在 896 上召回只剩 16–33%），而云端模型在 896/1280/1920 三档读数率持平——
  **本地 OCR 在原生分辨率下能读到快线在结构上永远读不到的字**，这本身就是 OCR 的价值证明。
- **D-47 OCR 生产定型（产品负责人拍板）：PP-OCRv6 tiny / OpenVINO CPU / 推理 2 线程 / detector `limit_side_len=1280` / 内存直传。**
  依据（4a-2 V1，48 配置 5760 次调用）：detector 原图 / 1536 / 1280 三档召回完全相同（T 均 83.3%、H 均 84.9%），
  1280 是免费的；线程从默认降到 1 使平均 CPU 从约 0.56 核降到约 0.30 核，2 线程取中；
  H（small-det + tiny-rec）召回只高 1.6pp 而稳态 RSS 约 698MB 超预算，落选。
  **输入路径只能是内存直传**（D-46）：4a-2 里"文件"档只是探针从磁盘读素材的产物，生产中不存在这条路。
  4a-2 内存档 RSS 偏高是探针持有解码数组所致，4b 实测为准。
  其余优化（量化 TD-36、crop 缓存等 4c 机制）一律推后，先把功能做出来（原则 3）。
- **D-26 深线允许结构化输出，快线不允许（待 B-T8 复核）。** 深线不受延迟约束，输出 ObservationPatch（JSON）：本地引用 local:oN 只在当帧有效，
  绝不冒充实体 ID；事件 ID、时间戳、帧号、像素指标、OCR 数值、键鼠数据由代码填写，模型不得重生成。
  快线改 JSON 或 DSL 不省时间（键名重复反增 token），还需解析与降级路径——不动。

### 3.5 通用视觉 · 认知层（M5 重定案）

- **D-27 通用视觉的状态是"证据 → 主张 → 信念"，自然语言只在两端。** 视觉模型把像素写成证词；信念状态渲染成人读的 cognition.md
  与给模型的短视图；中间是带类型的记录和一个代码归并器。理由：散文作为唯一状态库经历两次有损转换，丢掉对象 ID、坐标、数值、
  持续时间、来源与不确定性；并发下按到达顺序追加的文字必然乱序。跨模型不存在文字之外的通道（视觉 token 只有同族模型能读，
  托管 API 不暴露），所以精确的东西走代码通道（OCR、键鼠、指纹、检测器），视觉模型只做开放世界语义。
  **它提升的是认知更新速度（数字与输入毫秒级入状态），不是反应速度**——反应速度受 TTFT 固定开销支配（§5.3）。
- **D-28 模型提出判决，代码执行法律。** 视觉模型与笔记员的一切输出只能产生候选或补丁；升格为 confirmed 只由归并器按策略执行。
  模型不得直接重写整份状态。
- **D-29 两个时钟；迟到证据只补历史。** observed_at 与 learned_at 分开存，按 observed_at 归位。T+10 的"42"在 T+13 才返回时，
  只能加强 42 的历史记录，不能把已经是 37 的当前状态倒退回去。context_version 用来识别"观察有效但依赖旧背景的推断已过期"。
- **D-30 独立印证按证据家族计数，不按句子数。** 同一帧派生出的 OCR 读数、快线复述、笔记员转述属同一 root_capture_id 家族，只算一组。
  T7 期"快线逐字抄 OCR 上下文"事故的本质就是同一根证据被数了两遍。
- **D-31 模型自报置信度不是概率。** OCR、追踪器与语言模型的 0.9 不是同一把尺。只用 observed / inferred / uncertain 三档；
  升格靠"证据类型 + 独立组数 + 稳定持续时长"。
- **D-32 状态变化不是翻案。** 42 → 37 是 superseded；"Boss" → "普通守卫"才是 retracted。混淆两者会让正常变化被当成矛盾。
- **D-33 实体身份与名称分离；宁可拆，不可错合。** 实体 ID 不由模型起的名字决定，名字只是候选属性，合并可撤销。
  错误拆分只让记忆零碎；错误合并把一个对象的行为记到另一个头上。第一版只给持续或反复出现超过一定时长的对象分配实体
  （时长待实测）。视角频繁转动的游戏里轨迹断裂是常态：**断轨不删实体；重现先建新轨迹，证据够了再保守认亲**。
  两个同模型无名牌的敌人在转身期间换位，画面里没有信息能区分——接受身份未知，记成"同类敌人"。
- **D-34 不同事实用不同裁决策略，不设全局阈值。** 首版只有三条：ui_numeric（两组时间分离的读数 → confirmed，新读数 → superseded，
  久未刷新 → stale）、ui_text、scene。其余一律候选，深读或两组独立印证才升格；玩家意图永远只是推测。阈值从 1080p 录像实测。
- **D-35 笔记本 = 两栏 + 一本隐形账，垃圾可以进来但住不下。** 见闻栏＝证据流（可错、不可改写、滚动淘汰）；认知栏＝信念状态的
  人读投影（须被印证才升格、可翻案）；隐形账＝ActionLog。笔记员只对语义层提补丁。**把关的时点是写入信念那一刻**，不是每一帧——
  错误观察最多毁一句吐槽，被写进信念才是灾难。通用模式没有真值来源，目标是**会自我纠错**，不是永不出错。
- **D-36 长期记忆是带类型的文字记录，不是图片，也不是散文。** 截图即读即弃（调试落盘除外）；留下的是 evidence.jsonl、信念快照、
  ActionLog 与游戏卡；observations.md / cognition.md 是渲染视图，丢了可重生。短期视觉特征（指纹、轨迹、embedding）只许在内存，
  会话结束销毁。文字记忆便宜、可审查、可迁移、可被纠错。
  **帧环形缓冲（已批准概念，随深线落地）**：为让深线回看"刚才那帧"，允许在内存保留最近 N 秒的 896 宽 JPEG（每帧约百 KB，
  60 秒约 10 MB；N 待实测），不落盘、不存原始位图、会话结束销毁。深线引用它时以 root_capture_id 指认。
- **D-37 游戏卡：每游戏一份跨会话持久记忆，在线渐进生成，零预置。** 首次遇到游戏时边玩边学、不在关键路径上：主菜单期做货架一查询
  与首批深读，之后每出现新场景簇补一次深读（最强档，不计成本）+ HUD 槽位归属 + 键位印证。
  **通用模式零预置游戏内容**：除窗口标题 → 身份映射外，任何游戏专属信息只能在运行时从货架一查询或观察中获得。
  初始化只查货架一；深读自己的画面属货架二的合法来源；**剧情类内容一个字不查**。录像重放只是验收台，跑同一套代码。
  它不改变快线合同——快线仍写散文，只是多一段稳定背景。
  **货架一通路（已定，原 TD-29）**：以网关的联网调用为主、模型自身知识兜底，不接独立搜索 API；每游戏一次、结果缓存进卡；
  按 a→b→c 尝试、每步测量。初始化的深读用单次更强的多模态档位（vision_deep），不受快线档位与延迟约束。
- **D-38 场景指纹（感知哈希）是唯一进第一版的本地视觉特征。** 菜单 / 背包 / 地图近静止可聚簇；游玩画面聚不起来，正好是默认场景。
  只用于场景重识别与 scene_reset 印证，**不参与上传与区域提示决策**。CPU 微秒级，零依赖。
- **D-39 本地 GPU 允许，但有硬预算。** 任何本地视觉组件（轨迹、embedding 等）必须：显存与占用上限可配、可一键关闭、
  真机实测对游戏帧时间的影响并写入本文件、未实测不得常驻。首版主线不含 GPU 组件；本地短时轨迹为条件任务 B-T11。

### 3.6 知识边界与评测

- **D-40 两货架规则。** 分界线是"玩前可得 vs 玩后所见"。货架一·开箱即知（可 web search，每游戏一次，本地缓存）：类型、世界观一句话、
  操作与 UI 惯例、机制体系、社区通用术语、默认键位表——机制可以查到饱。货架二·亲眼所见（只能从观察与推理积累）：剧情、角色命运、
  结局、地图与世界内容。游玩中允许由观察触发的按需查询：屏幕上出现了不认识的名词才去查那个词条，按词条缓存、限频。
  剧透不做硬限制，只在提示词中 discourage。
- **D-41 评测题干必须与生产输入同构。** 出题人的先验不得以任何形式进入模型可见内容——不得有题目专属提示词、不得把题号或类别送进消息体、
  区域提示必须由检测器机械生成。本项目曾因此使抗幻觉题整题作废（F-14）。
- **D-42 通用提示词（含示例）不得含任何具体游戏的机制、道具、事件名词**，只允许"某界面元素出现 / 某区域亮度变化"这一抽象层级（F-16）。
  按线分提示词合法；按游戏分永远非法；游戏信息只以数据身份进上下文。

### 3.7 文档与流程

- 三份 md 的修改权限：coding agent 默认不得修改任何内容。唯一例外须同时满足：产品负责人明确打字同意；任务规格附精确的
  旧文本 → 新文本替换组；agent 只执行替换一个字不得自行增删；任何一组无法逐字匹配立即停止报告；完毕后报告新的第二行与总行数。
  设立理由：曾两次因文档未正确更新使 coding agent 连续九个任务读到过期内容。
- 架构师每三个任务必须更新本文件一次，判断标准是距上次更新过了几个任务，不是内容是否稳定（F-13）。
- 禁止修改（需在提交中带上）：/docs 下全部文件；/backend/data/<game_id>/ 下全部资产（测试与实现不一致时改实现，不许改资产迁就）；
  /backend/prompts/ 下的措辞（代码只负责加载与拼接）。
- 提交进仓库的测试不得依赖未提交的机器本地数据：所引用的本地文件缺失时必须以明确理由 skip，不得 fail。
- 偏离本文件技术栈或分层职责必须在完成报告显式标出。

### 3.8 编码规范

- Python 全量类型注解；跨模块数据必须 dataclass 或 pydantic，禁止裸 dict
- 后端只绑定 127.0.0.1；端口 8737 前后端各只允许一处常量
- 任何入库产物不得包含真实玩家身份（player.name / 各 steamid 及 previously/added 对应项一律占位符替换；武器名地图名程序名不属身份信息，
  不得误脱敏）。仓库是公开的
- 禁止裸 except 静默吞异常；网络测试必须注入假客户端不得真联网
- 读取玩家数据段前必须确认数据主体是本人（各游戏的主体识别规则见其 md）
- evidence.jsonl 只追加，任何组件不得改写已写入的证据；信念状态只由归并器写入，测试须断言单写者；
  模型输出进入信念层前必须经归并器验证，拒绝的写入 ActionLog
- Tauri v2 权限默认全关，新增能力在 capabilities 显式最小授予
- 窗口尺寸位置一律逻辑像素（DIP）按 scale_factor 换算，禁止硬编码像素补偿
- 前端 Prettier 统一格式化；Rust 过 cargo fmt --check；前端视觉组件独立模块，外部不得直接访问其内部 DOM
- 回归测试对已修复缺陷加 ROOT CAUSE 注释，写明当年怎么错的

---

## 4 反复出现的失误模式（引以为戒）

本项目实际发生过、同类形状出现过多次的错误。每条：现象 → 规则。

- **F-01 把未验证的分析当成诊断。** 曾按回合号分组统计而录制含三局比赛，虚构出不存在的缺陷并施工一轮。→ 从数据得出结论前先核对数据的组织结构。
- **F-02 声称测试失败是"既有问题"却不复现。** 曾把自己引入的回归报成"非本轮引入"。→ 必须在上一个提交实测复现并附输出，否则按本轮引入处理；"我没改那个文件"不构成理由。
- **F-03 在既有时序机制前后插入新等待，却不重算时间预算。** 曾把释放窗口压成 2.5 秒导致整句丢失。→ 新增等待 / 延迟 / 收敛时重推所有相关时间预算。
- **F-04 删除清单靠印象，不先查引用。** M4-a 曾把测试依赖的 5 份回归资产当垃圾删除。→ 删除前全仓库检索引用，逐个归类后再定清单。
- **F-05 用自己临时想的关键词自证"没问题"。** → 先定清单，再拿它去扫，不得反过来。
- **F-06 验证环境不干净。** → 验证前确认 `python -c "import pet; print(pet.__file__)"` 指向本次克隆。
- **F-07 凭直觉给阈值。** → 规格中任何阈值必须附实测依据，或明写"此数待实测确定"。
- **F-08 重命名标识符时漏改引用方；同一份清单存两处抄本。** → 被提示词或数据文件引用的标识符必须同批同步；两处清单必须有测试断言其一致。
- **F-09 加新机制前不查既有机制。** → 先说明查过什么、为何现有机制不够。
- **F-10 规则与内容自相矛盾 / 自查范围不明。** → 自查方法必须写明匹配范围。
- **F-11 需要人工判读的指标被自动统计代替。** → 把"逐项给出判定与理由"写成硬性验收项。
- **F-12 底层行为靠读代码推断。** → 涉及系统 API / GPU / 网络 / 外部服务时，规格写成"按 a→b→c 顺序尝试，每步测量，够用就停"。
- **F-13 架构师连续用"等定型了一起写"推迟 AGENTS.md 更新。** M4 关闭后连续 14 个任务未更新。→ 每三个任务必须更新一次（§3.7）。
- **F-14 评测的题干泄露了答案。** 规格批准了"题目专属提示词"，抗幻觉题的题干里写着"若无显著事件则必须返回空"，整题作废。→ D-41。
- **F-15 采购了条款禁止本用途的服务套餐。** 某订阅套餐明写"仅限编程工具中交互式使用，禁止用于自动化脚本与应用后端"。
  → 本项目是自动化后端，任何"仅限交互式使用"的套餐一律不可用，购买任何额度前先读用途条款。
- **F-16 通用提示词的示例走私了具体游戏内容。** T7.3 修订在示例里写了"闪电、手电筒"，等于告诉模型往这类事件上靠，真值回归 2/5。→ D-42。
- **F-17 为滤噪声加的时间门控把真实事件成批滤掉。** "连续两轮才算数"在 1 秒采样下恰好滤掉只持续一帧的真实剧变，漏检十倍。
  → 任何门控 / 滤波 / 收敛窗口，设立前必须先量目标事件的持续时长，确认事件能活过这道门。
- **F-19 评测的答案键由参赛者生成。** B-T4a 的 OCR 真值集初稿是用候选之一（WinRT）的输出生成的，再拿它评 WinRT，
  得到"100% 准确率"——只证明了评测管线能复现自己的输出。独立重评后真实召回 58.3%。与 F-14 同形状：答案来自被考者。
  → **真值必须独立于全体候选**（人工穷举、或至少来自不参赛的第三方来源），并在任何横向比较之前完成校对。
- **F-18 提出优化前不对照本文件的实测账本。** 架构师曾提议砍掉快线【全局画面】段以"减半延迟"，而账本早已写明快线可见输出仅约 30 token、
  解码占比可忽略。→ 任何提速 / 降本主张必须先引用 §5 的具体数字，账对不上就不提。

---

## 5 关键实测数据（主干；CS2 专属数据见 CS2.md）

### 5.1 前端、后端、语音

- 前端：release 空闲 4.19% 单核、隐藏 1.12%；呼吸动画外置 + 8Hz 较优化前降 88.9%（单纯提升合成层反而更差）。
  静态约 408 MiB / 7 个 WebView2 进程，为固定成本。
- 后端：启动最坏 0.85 秒；稳定 50 MiB；30 分钟无泄漏；/gsi 中位 0.448ms、P95 0.670ms（接入模型后未退化）。
- 语音：20 字中文出声延迟最坏 0.42 秒；停止 1.01 秒内返回。`speak()` 先 `stop()`——**新句子掐掉正在播的那句，不是排队**（M3-T12 实测确认）。
  一句 19–30 汉字约 2–3.5 秒说完；加上模型 0.5–1.5 秒，**一句话从选中到说完约占 4 秒**——任何发言策略都要对着这本物理账设计。

### 5.2 截屏与画面变化检测（M5-T1 / T1.5 / T3 / T4 / T5）

- WGC（zbl 0.7.1）按精确 HWND 抓单窗口：抓帧最大 38 毫秒；六指标含块级统计合计中位 9.4 毫秒（1920x1080）；
  1 秒轮询单核 CPU 约 3.3%，2 秒档约 2.1%
- **单一平均振幅指标不可用**：环境动效（雨、浪、植被）与局部文字变化的失效方向相反，不存在同时满足两者的阈值 → 块级自适应噪声地板
- **必须容忍数秒级的空帧窗口**：曾观测到击杀后 5.86 秒才有下一帧；WGC 无新帧时必须非阻塞取帧，阻塞式会把探针锁死 49–90 秒
- 兼容性债：某游戏在 WGC 会话存续期间游戏内光标消失，跨游戏负对照未复现，只写成"该游戏与当前捕获会话的交互异常"，不写特判
- **P=2 的漏帧证据**（1403 帧真实录像）：P 从 1 提到 2，局部剧变漏检 11.6→115.9、输入活动漏检 49.0→153.3；
  存在门控前 65.97% 块变化被判 no_change 的轮次；探索录像连续 5.0 秒零上传。T5b 修正后四组录像 no_change 全线大降
  （探索会话 200→66），上传率 46–58%（约 $0.1/小时），简化后判定合计中位 4.7 毫秒
- 输入与画面时间线错位约一个轮询周期（真值后移一帧使镜头移动召回 29%→38.7%）
- **第二轮网格（108 组 × 16 角色，1728 行）与参数定案依据**：定案组合对比现值——字幕剧变漏检 3→0、总漏检 37→4（残余全在卡牌会话）、
  世界事件命中 14→19、暴雨静止上传率同为 2.0%（k 从 2.5 降到 1.0 地板全程稳住）、动态会话成本 $0.24→$0.28/小时。
  三个入围组合 margin 全部 =4/255：安静块的唯一防线是加数不是乘数（中位数近零时 k 乘不出门槛）
- 燕云十六声"纯静止"样本经逐帧复核重定性为"零输入动态世界"：光照转变段 23 帧 21 帧真实触发、字幕更新属应抓事件——40.99% 不是误报率。
  **真阴性锚点只剩 B-idle（灰区暴雨静止）与会话 A 的静止段**
- 互相关滞后测量：四个过渡段测出 2.1 / 7.0 / 2.1 / 23.1 秒、相关口径含负值——过渡段不是干净阶跃，当前素材测不出可信常数（TD-22）

### 5.3 视觉模型（M5-T2 系列 11 题考卷 × 多模型；T7 / T8）

- **TTFT ≈ 1324 毫秒固定开销 + 0.97 毫秒/token**，三个不同规模的模型在同一上传宽度下 TTFT 几乎相同（896 档：2061 / 2078 / 2077 毫秒）——
  **该固定开销来自管道而非模型计算，换模型救不了它**
- 推论：**上下文每多 1000 token 就多等 1 秒**；提示词组装必须"稳定前缀在前、变化部分在后"
- 压缩输出文本对提速几乎无用（快线可见输出仅约 30 token，解码占比很小）；其价值在记忆质量而非提速
- 上传宽度是唯一有效的提速杠杆：1280→640 砍掉约 1 秒，但 **640 会读错文字，896 是质量下限**。原生分辨率在快线出局（中位 9.4 秒）。
  **上传宽度曲线**：896 / 1280 / 1920 读数率持平（10.9 / 10.1 / 11.3%），1920 多付 0.5 秒 TTFT 与 85% 费用——**896 定案，瓶颈在模型内部**；
  录制宽度已改 1920 默认
- 地理位置是最大的单项：亚洲上游 TTFT 中位 2.71 秒，美国上游 1.27 秒，差 1.44 秒。但所测美国 serverless 有冷启动长尾
  （P90 18.8 秒、最大 77.8 秒），不可用于每秒一帧
- **必须锁定单一上游**：未锁定时最大延迟 304 秒与 636 秒，锁定后降至 6.09 秒
- 推理 token 会计入输出预算：某模型强制开启推理，60 token 档 22/22 截断、8 次可见输出为空。**这是配置问题，绝不能记成模型能力不足**——
  跑卷工具须自动检测截断率并以宽松额度重跑、显著标注"该档不可比"
- 整卷四模型 132 次调用总花费 $0.49；便宜档单帧约万分之一美元。**成本不再是本项目的设计约束**
- **T8 双模型矩阵**：现任快线模型冻结回归真值 5/5；外部候选 2/5，但 1080p 下读数字明显更强、TTFT 中位仅 500–760ms——保留为常设对照
- 真机快线延迟中位曾降至 1.55 秒（删上下文后，未锁变量，不下因果结论）
- 四段 1080p 片段答案键（会话背景 + 现场记录 + 判卷专名表，专名表与模型上下文结构隔离）已入库

### 5.4 本地 OCR（M5-T7.10 / T7.10b / B-T4a）

**引擎横评（B-T4a，40 帧 1080p，324 条独立视觉参考 anchor，整行精确匹配）**：

| 引擎 | 严格召回 | 数字召回 | P50 | P90 | RSS | 置信度 |
|---|---:|---:|---:|---:|---:|---|
| WinRT（原生分辨率） | 58.3% | 40.8% | 75.5ms | 141.6ms | 121 MB | 无 |
| PP-OCRv4 mobile / OpenVINO CPU | 85.8% | 71.1% | 234.9ms | 385.5ms | 1151 MB | 有 |
| PP-OCRv6 small / OpenVINO CPU | **88.6%** | **71.1%** | 258.2ms | 575.0ms | 1153 MB | 有 |
| PP-OCRv6 tiny / OpenVINO CPU | 83.3% | 64.5% | **150.8ms** | **276.7ms** | **804 MB** | 有 |

分类召回（v4 / v6 small / v6 tiny）：**HUD 数字 56.9 / 60.3 / 60.3%（全场最弱）**、字幕 75.5 / 85.7 / 67.3%、
菜单 97.7 / 97.7 / 91.4%、密集文字 93.3 / 95.5 / 95.5%。WinRT 的 HUD 数字只有 27.6%。

- **延迟与文字行数强相关**（r = 0.77–0.90），尾延迟来自"对大量文字框逐一识别"：一帧 61 框的密集菜单，检测 141ms、识别 249ms。
  优化方向是减少重复识别，不是删坐标（坐标归一化 P50 < 0.2ms，可忽略）
- **整图降到 896 是错的**：候选在 896 上召回只剩 16.3–32.7%，"更快"来自少检测少识别，不是同召回下更快
- 1920 vs 2560 无普遍优劣（v4 −7.2pp、v5 持平、v6 small +4.6pp），且单轮时间有轮次/温度噪声——未交错重复前不下因果结论
- Windows AI TextRecognizer 在本机不可用：无匹配 NPU、无 Python projection、unpackaged 进程缺 systemAIModels capability
**收口实测（B-T4a-2 V1，48 配置 × 5760 次调用，40 帧 1080p，同一批 324 条 anchor）**：

- **线程数是 CPU 的主杠杆**：默认→1 线程，T（v6 tiny）0.56→0.28–0.30 核，H（small-det+tiny-rec）0.71→0.38 核；
  延迟代价可接受（T 单线程 P90 393–412ms，仍在 500ms 内）
- **detector 尺寸买不到东西**：原图 / 1536 / 1280 三档召回**完全相同**（T 均 83.3%、H 均 84.9%），延迟也在噪声内。
  取 1280 的理由不是省时间，而是**让检测成本与屏幕分辨率脱钩**（1440p / 4K 录制时不随像素膨胀）
- **内存路径省约 10ms**（JPEG 解码与框架开销）；探针里内存档 RSS 反而更高是**探针自身持有数组**所致，不是生产性质（D-46）
- 稳态 RSS：T 约 588 MB（达标）、H 约 698 MB（超）。峰值 RSS T 约 883 MB，只记账
- **测量瑕疵（已知，向上偏）**：收尾 CPU 取样的开销计入了核心秒但未计入墙钟，放大"峰值并发核数"比值；
  1 线程测出 2.0 核即由此而来，属边界不可分辨，不判失败
- `eval-reports/m5-b-t4a2/runs/` 已混入中断的 V2 原始数据，**不得用于逐帧复核**；汇总 `report.md` 与 `metrics.json` 是完整的 V1

**引擎定型（产品负责人拍板）**：**PP-OCRv6 tiny / OpenVINO CPU，detector 上限 1280，内存输入，线程数可配（默认 1，
待 4b 在游戏中实测后定）**。放弃 H 的 +1.6pp 召回换 110 MB 稳态内存与更高 CPU。

**早期 WinRT 数据（M5-T7.10 / T7.10b，口径不同，仅作历史参考）**：中文可用；**640 宽素材零输出，分辨率是硬门槛**；
两遍式首遍留出召回 85.2%（58ms）、彩色二次 96.3%（247ms）——该"召回"为引擎自评口径，已被 F-19 推翻，不得再引用。
选择性二次 OCR 原型（持久工作进程 + 缓存差分 + 预算）已落地但**未必会用**（D-45 先最简后加机制）。
WinRT 无置信度、多次识别可互相矛盾：OCR 输出是**证词不是事实**，稳定性由 D-34 的策略按跨帧一致性裁决。

### 5.5 语言模型（当前 CS2 档位锁定单一上游）

- 事件 P95 约 0.8 秒；约 ¥0.5/小时；长尾可达 15.7 秒——线上必须配短超时与模板回退
- **输入几乎免费，输出是延迟大头**：多 1200 输入 token 仅 +0.119 秒，多 87 输出 token +2.234 秒。提示词想写多长写多长，生产坚持单行输出
- 该模型必须显式传 `reasoning_effort="none"`（`low` 会输出英文推理并耗尽预算）
- **固定种子会让温度失效**：多样性采样不传种子；正式评测保留种子以复现
- 上游不锁定则延迟不可比（OpenRouter 单次运行出现过八家分发）

### 5.6 认知层

暂无实测。B-T2（指纹聚簇）、B-T3（游戏卡）、B-T7（笔记员）、B-T8（背景注入 A/B）起逐项填入。

---

## 6 现状（覆盖语义）

### 6.1 里程碑总览

已实现：
- **M1 桌面宠物**：置顶悬浮窗、拖动、显隐热键、托盘菜单、表情、气泡、系统语音（可中断）、WebSocket、待机播报
- **M2 看得懂 CS2**：GSI 接入与自动安装、录制回放、会话与主体识别、事件检测、发言策略、模板话术、对局生命周期
- **M3 每句话由大模型当场生成**：代码产事实句 + 模型只管文风、词库与闸门、线上接入与回退、评测基建、连杀收敛。产品负责人真实对局验收
- **M4 多游戏地基**：删 4.4MB 过程产物并产出模块清单；端口 v1 定稿；一次性重构落地——core / games/cs2 / eval 三层分离、全目录英文化、
  性格与游戏知识分层、每游戏独立配置段与模型档位、分层警报器上线。迁移前后：35 场景选中序列逐字节一致、提示词逐字节一致、通过数 414>409

未实现（滚动细化，只细化当前里程碑；主线与适配线并行）：
- **M5 通用视觉**（默认关闭），三段只含通用模式，CS2 视觉与战雷视觉归适配线：
    - M5-A 看得见（收官冻结）：截屏 + 快线观察 + 窗口标题查表识游戏 + 滚动观察日志。验收物是观察日志；真机 10 分钟判词并入 B-T10
    - **M5-B 跟得上（当前）**：游戏卡 + 证据流 + 归并器与信念状态 + 笔记员提补丁 + 背景注入 + 深线。验收物是**游戏卡与 cognition.md**：
      玩 20 分钟后打开两者，对照自己的记忆判每一条真伪，并抽查任意一条认知能否沿证据 ID 追到具体帧。
      此时还不能：开口（M5-C）；联网查货架一之外的内容；Live2D；跨局翻案（M6）
    - M5-C 开口：发言仲裁读信念快照 + 最新快线证词 + 事件卡，只写"何时开口"；说话链路 M3/M4 早已就绪
- **M6 跨局长期记忆**：游戏卡已是种子（B-T2 起持久）；剩下会话信念快照的持久化、下次开局装载与跨局翻案
- **M7 神经网络语音**：必须保留可随时中断，延迟不得明显劣于系统语音
- **M8 语音对话输入**
- **M9 外观自定义、心情系统与设置面板**（设置面板：性格 / 外观 / 适配管理的 UI 壳）
- **M10 打包分发，朋友可安装 —— MVP 完成线**

Post-MVP：适配形态升级为独立分发（A→B→C）、设置面板的适配下载页、AI 玩杀戮尖塔、AI 玩文明 6（"AI 代玩"与本产品端口不通用，不预留设计）。

### 6.2 当前里程碑 M5-B：任务序列

M5 重定案一句话：通用视觉的状态不再是"散文笔记本"，而是"证据 → 主张 → 信念"的结构化状态（§3.5）。
**执行策略：传感器先行，快线最后重设计（D-44）。** 先把所有本地/前置输入做齐——检测器（已有）、OCR、键鼠（已有）、
场景指纹、游戏卡、深线原语——每落地一个就从快线职责里减去一项；等减完再一次性重定快线合同（B-T8）。
在此之前快线合同（D-22）、检测器参数（D-15）、上传宽度 896 一律不动。
旧 B-T1（两栏笔记本骨架）规格已作废（未交付）；新 B-T1 整份取代。

任务 ≤ 一天粒度、单一职责、可独立回滚；任何阈值一律"待实测"。推荐顺序即列序；产品负责人暂无时间判游戏卡时 B-T4 / B-T5 可先于 B-T3。

| 顺序 | 任务 | 内容 | 产品负责人怎么验 | 依赖 |
|---|---|---|---|---|
| ✅ | **B-T1 证据流骨架**（`301f825`） | core/belief/ 的 EvidenceEvent / EvidenceStore / 渲染器；三类证据齐全，md 与改前逐字节相等且可从证据重生 | 已验收 | — |
| 1 | **B-T4 OCR 接管线**（4b 执行中） | **4a 选引擎**（✅ 完成，`a6be790`）：真值集初稿被 F-19 推翻后以独立参考重评；三款 OpenVINO CPU 候选入围，WinRT 因召回 58.3%/数字 40.8% 出局；判据据此改写（D-45）。**4a-2 收口**（✅ 完成）：线程数是 CPU 主杠杆；detector 尺寸对召回零影响；定型 **v6 tiny / OpenVINO CPU + detector 上限 1280 + 内存输入 + 线程数可配**（§5.4）。**4b 最简单遍式**（执行中）：按 D-47 全帧、单遍、无 crop、无预算、无爬坡接进 adapter，只保留行差分；只跑通过检测器的帧与心跳帧；以 source=ocr 进证据流；**《文明 6》运行中开关 OCR 对照帧时间与 1% low** → **停下看数据**。**4c 按需加机制**（条件）：仅当 4b 超标才加，每加一项报告换来多少毫秒/多少召回；若 4b 达标则删除 ocr_selective.py 中未被使用的 crop/预算/爬坡代码。全程**不碰快线提示词**（D-44）。放宽 models.py/render.py：新 kind、同帧多条同 kind。顺带从重放的 evidence.jsonl 算出**输出解码速率**补进 §5.3 | 4a 读对照表（含漏读/错读样例）拍板选型；4b 重放四段 1080p，抽查 OCR 条目与画面对得上、中位耗时是否达标；4c 只在必要时发生 | B-T1 |
| 2 | B-T2 场景指纹 + 游戏卡骨架 | 每张选中帧用现有 Pillow + numpy 算感知哈希（不加 imagehash），按汉明距离聚簇（阈值待实测）产出 scene_fingerprint 证据并印证 scene_reset；建 backend/memory/<game_id>/ 游戏卡（json + md，加 .gitignore），此时簇无标签、HUD 槽位为空、货架一字段为空。查表只管身份；指纹不参与上传与区域决策。**顺带**：渲染器 close() 与重放行构造对不完整帧组记 WARNING 列出帧号，不再静默跳过 | 四段 1080p 录像：gamecard.md 列出场景簇、次数与时段；菜单 / 背包 / 地图各自成簇，游玩画面不成簇；scene_reset 印证率 | B-T1 |
| 3 | B-T5 鼠标聚合证据 | input_telemetry 把鼠标位移按 100ms 窗口聚合为方向 + 幅度，以 source=mouse 进证据流并同步进快线【玩家输入】行；游戏卡有角度常数、玩家提供了灵敏度且普通视角时附绝对角度。只出聚合信号。放宽 root_capture_id=None 的非帧证据 | 重放一段你记得转了约 90° 的片段：证据显示方向与幅度；若校准，角度在容差内（待实测） | B-T1（校准字段依赖 B-T3，可后补） |
| 4 | **B-T3 游戏初始化（在线渐进学习器）+ 深线原语** | 首次遇到游戏时在线、渐进、不在关键路径上：主菜单期做一次货架一查询（每游戏一次、缓存）并深读主菜单簇；游玩中每出现新簇补一次深读（新建 vision_deep 档位：最强模型、高 token、结构化输出）得场景标签与 HUD 区域候选；OCR 稳定行归 ui:rN 槽位与语义角色候选；动作键事件后短窗口（待实测）内簇跳变 → 键位候选，计数印证；货架一事实以 source=init、modality=inferred 入卡。卡持续变厚，下一局直接装载。深读调用原语在此建成，供 B-T9 复用。录像重放只是验收台。每游戏首局约 10–20 次深读 | **第一场大考**：四张游戏卡逐字段判场景标签、HUD 槽位角色、键位；货架一事实有无越界（剧情类出现即打回）；卡里没有任何预置内容 | B-T2、B-T4 |
| 5 | B-T6 归并器与信念状态 v1 | core/belief/reducer：机械来源的 Claim 标准化（OCR → numeric_value / reads_text，指纹 → scene，键鼠 → doing / changed_from）；BeliefSlot 生命周期；三条策略 ui_numeric / ui_text / scene（参数化）；证据家族计独立组；按 observed_at 归位迟到证据；ActionLog；快照版本号；cognition.md 机械段渲染。回归测试含 ROOT CAUSE：迟到的 42 不覆盖 37、状态变化不判矛盾、同帧派生只算一组 | 四段录像重放：cognition.md 机械段对照现场记录；随机抽 5 条认知沿证据 ID 追到帧号 | B-T1–B-T5 |
| 6 | **B-T7 笔记员 v1（提补丁）** | 文本档位、每 2 分钟（可配）：输入 = 上版本以来的快线 / 深线证词 + 信念短视图 + 未解矛盾；输出 = StatePatch（每条引用证据 ID）；归并器逐条验证、拒绝原因入账；cognition.md 语义段渲染（实体与命名 / 正在发生 / 存疑与矛盾 / 待查） | **第二场大考**：语义段对照记忆判真伪；看被拒补丁的数量与原因；能自动算的算：数值遗忘、改名次数、误判矛盾、迟到污染、可追溯率 | B-T6 |
| 7 | **B-T8 快线重设计**（吸收原 B-T1.5） | 传感器齐备后重定快线职责（D-44）：先用四段录像量出"OCR / 指纹 / 键鼠 / 游戏卡已经答了哪些、快线还剩什么"，据此改提示词与上下文（游戏卡短视图 + 认知短视图作稳定前缀，附 context_version；【推测】按数据决定去留），并把候选输出形态在冻结 11 题 + 五个重放角色上 A/B（形态候选：现状 / 减法 / 方位标记散文 / 事实元组，**reads_text 列不再由快线承担**）。同批评估发送频率是否可降、哪些事件类型可不等快线 | A/B 表：核心覆盖、编造率、考卷分、输出 token、TTFT、端到端增量（对着 D-43 的 3–4 秒账）。合同改不改、【全局画面】去留，产品负责人定 | B-T3、B-T4、B-T5、B-T6 |
| 8 | B-T9 深线 v1（在线触发） | 复用 B-T3 原语；三种触发（归并器待查、笔记员提问、闲时补课）；每小时预算上限（初值 6，待实测）；结果以 source=deep 进证据流，local:oN 只在当帧有效；待查队列落盘供闲时消化；帧环形缓冲供回看（D-36） | ActionLog 显示深读升格或解决了哪些槽位；预算不超 | B-T6、B-T7 |
| 9 | B-T10 里程碑验收 | 真机 20 分钟：一个游戏从初始化到游玩；读游戏卡与 cognition.md；M5-A 的 10 分钟判词并入；AGENTS.md 双里程碑关闭大更新 | 上述验收物全套 | 全部 |
| 条件 | B-T11 本地短时轨迹探针 | 开启条件：B-T8 A/B 显示实体命名稳定度仍不达标（目标由产品负责人定）且失败源于转视角后的身份丢失而非命名。内容：本地高频低分辨率帧、按 frame_seq 串行的短时 tracker、TrackletEvent 进证据流、特征只在内存；GPU 硬预算与帧时间影响实测先行 | 探针报告：帧时间影响、断轨率、保守认亲的正确率 | B-T8 数据 |

**编号不重排**（避免与已下发规格错位），执行顺序以"顺序"列为准。**B-T1.5 已撤回**：在传感器齐备前优化快线格式，是为一份即将作废的职责清单做优化（D-44）；其变体、判据与判卷表设计并入 B-T8。

成本预估：初始化每游戏一次性约 $0.1–0.3；笔记员 ≈ $0.01/小时；深线 ≈ $0.005/小时；快线不变 $0.28/小时。成本不构成约束。

### 6.3 排队、并行线与适配线

- 并行：T7.8（429 冷却，TD-25）与 B-T1 并行下发，B-T10 真机前必须落地
- 排队：【推测】去留并入 B-T8（改冻结提示词要跑回归，与重设计一次付清）；视觉美国区端点仍等服务商账号人工审核（TD-20）
- 适配线（第二位开发者，代码审查归架构师）：战争雷霆实时数据适配**现在可开工**（开工文件 ADAPTER_GUIDE.md；对标 CS2 的 M2+M3 深度；
  数据源为游戏自带 localhost:8111）；战雷视觉适配与 CS2 视觉适配（OCR + 地图识别等，落在 games/cs2）在 M5 之后；
  CS2 地图战术建议（原 M3-T14）随时可做，触发于回合开始，可预生成。CS2 待办清单见 CS2.md

### 6.4 技术债（编号固定，已销条目保留编号）

1. `prompts/cs2/gate-requirements.json` 与 CS2 代码中的事件名 / 场景标签是两份无测试同步的清单。偿还：下一个 CS2 任务顺带加断言测试
2. 各游戏的配置模型仍定义在 core/config.py。偿还：战雷适配开工任务中移交各游戏包
3. 频率保险丝（§2.2 之三）已定设计未实现。偿还：战雷适配接入主干之前
4. 语句多样性不足、词库利用率待提升。偿还：CS2 适配内做，时机自定
5. `games/cs2/eval/fact_sentence_audit.py` 与 `template_rules.py` 的脏字单字项会命中常用词（「操作」命中「操」）。偿还：下次动 CS2 闸门数据时
6. speaker 的 30 汉字输出硬上限比产品目标 19 字长 50%，不得因沿用成为事实标准。偿还：M7 用语音时长实测定
7. Steam appmanifest 的 installdir 未做路径归属校验。偿还：M10 前
8. test_idle_broadcast 仍真实等待且用 model_construct 绕过校验。偿还：M10
9. speech.py 的 PowerShell -EncodedCommand 组合可能被杀软拦截；每次朗读新建进程是否改常驻待实测。偿还：M10 前验证；常驻化并入 M7
10. requirements.txt 未分运行 / 测试依赖（测试链 13.9 MiB）。偿还：M10
11. 跨域白名单只覆盖开发来源，打包后来源未确认。偿还：M10 前必须复核
12. 无 LICENSE 且 pyproject 未声明（依赖许可已核实无传染项）。偿还：M10 前
13. pet.ts 同时承担渲染与拖动发起。偿还：M9 更换外观时
14. 前端与 Rust 测试覆盖薄。观察中，出现回归再引入最小手段
15. OneCore 语音测试在无中文语音的机器上失败 / 跳过，环境问题或测试设计待确认。偿还：M10 前
16. prompts/cs2/brother.md 与 caster.md 仅模板路径使用（双性格已取消），与 [games.cs2.personality] 段的去留待定。偿还：下次动 CS2 模板路径时
17. 转视角后的地板恢复盲区（推测未证实）：连续数秒剧变会顶高中位数，结束后 5–10 秒内门槛偏高可能吃掉小事件。
    偿还：在 explore-3a 上测量"转向结束后 10 秒"的判定分布；若证实，剧变帧不计入地板历史窗口
18. 区域提示稀疏上限（现 region_focus_max=0.50，吸收原 region_sparsity_max=0.25）仍为初始猜测，与评测工具中的同名阈值须同源。
    偿还：扫 0.15 / 0.25 / 0.40 后统一
19. zbl 0.7.1 是相对小众的新引入依赖，维护活跃度与长期可靠性未评估。偿还：M10 前
20. 视觉链路的美国区端点因服务商账号人工审核未能实测（探针在 HTTP 403 停止），推理开关字段名亦未能实测。偿还：审核通过时重跑探针
21. ~~快线超时与丢帧策略~~ 已随 M5-T7 实现（5 秒超时、超时丢帧不重试、并发上限 4）。已销
22. 时间线滞后常数未定：zbl 不暴露呈现时间戳，互相关在现有素材上测不出可信值。已降级为按需：真需要真值级对齐时录专门的阶跃实验
    （静止中只切手电 × 10 次，两分钟）；喂模型的用途不受影响
23. 输入隐私机制：动作键白名单的结构隔离已随 M5-T7.4 在生产管道实现（前半已偿）；打字风暴静默、输入法合成静默仍未实现。
    **偿还硬线：任何分发（M10）之前必须全部实现并把全键盘开关移除或锁死**
24. 中心加权 margin：准心永远在画面中心，中心块 margin 压低、边缘抬高，提高"准心里出现小目标"的检出率。数值须用含此类事件的录像经 --replay 校准。
    偿还：有合适录像时
25. 429 冷却与错误元数据（T7.8，规格已出未执行）：客户端保留 error_type / provider_code / Retry-After / X-Generation-Id；派发级冷却 + 指数退避
    （非重试，丢帧教义不变）。T7.7 曾因缺元数据无法归因 53 次 429。偿还：与 B-T1 并行下发，B-T10 真机前必须落地
26. 锁定单一上游的可用性代价已实测（限流时整段失明、零故障转移）。受限回退清单暂不建——合格名单仅一家。重开条件：第二合格上游出现或 TD-20 落地
27. 本地 OCR 接管线：原型已落地（T7.10b），定位定案为**证词不是事实**——以 source=ocr 进证据流，稳定 HUD 由 D-34 策略升格，瞬时文字保留单帧候选。偿还：B-T4
28. 信念层阈值全部为待实测占位：指纹汉明距离、实体存续时长、ui_numeric 印证组数与 stale 时长、键位印证窗口、深线每小时预算、游戏卡短视图长度。
    偿还：各自任务内用四段 1080p 录像实测定值，规格不得写死
29. ~~货架一联网查询通路未选型~~ 已定：网关联网为主、模型知识兜底、不接独立搜索 API（D-37）。已销
30. `test_frames_call_model_and_are_logged_in_frame_order` 用真实 sleep，CPU 满载时踩到 5 秒超时而失败；在 B-T1 之前的提交上同样复现，
    与 TD-8 同类。偿还：M10 前改为注入时钟；在此之前验收时按"隔离重跑一次通过即通过"处理
31. OCR 真值集：现有 `data/generic/ocr-truth/truth.md` 是 WinRT 生成的草稿（含 `"0 佗 E 日 劊 0 ，"` 一类乱码），
    已被 F-19 判为不可用，**不得作为回归资产使用**。当前横评用的是独立视觉参考的 324 条 anchor，可作相对比较，
    不能算精确率。**产品负责人自行人工穷举**（不下发给 agent），重点是 HUD 数字那 10 帧——该类召回最低且是
    ui_numeric 唯一粮食。穷举完成前不得给出正式 precision，也不得最终定型引擎；4a-2 与 4b 可在此之前进行
32. HUD 数字召回全场最低（三款候选 56.9–60.3%，WinRT 27.6%）。结构性解法不是换引擎或加机制，而是
    **游戏卡学到 HUD 槽位后对小框定向识别**（全帧 85% 的召回已足以发现槽位，自举链成立）。
    偿还：B-T3 之后评估；在此之前不得为 HUD 数字单独优化 OCR
33. `eval-reports/*` 已被 .gitignore 覆盖，但 B-T4a 强制提交了 2.3 MB `metrics.json` 与 2276 行报告。
    M4-a 曾为同类过程产物做过一次 4.4 MB 清理。偿还：4a-2 提交中从版本库移除（保留本地），只留 ocr-truth/ 与 audit/probe.py
34. OCR CPU 占用：离线核心秒数已实测（§5.4，前半已偿）；**游戏运行中的帧时间 / 1% low 对照仍未测**，
    而这是 D-45 判据 2 的真正门。偿还：4b 必测，未测不得判定达标
35. 4a-2 的 CPU 取样有已知向上偏差（收尾取样计入核心秒未计入墙钟），且 1 线程测出 2.0 核峰值自相矛盾——
    线程限制可能未覆盖全部阶段（检测/识别/前后处理可能各有线程池）。偿还：4b 同时设 OpenVINO 推理线程与
    `cv2.setNumThreads` / `OMP_NUM_THREADS`，并实测线程上限是否真的生效；若仍不生效，报告并由架构师决定
36. OCR 模型内存偏重：v6 tiny 稳态 588MB / 峰值 880MB，加前端固定 408MiB，整机占用超 1.2GB。
    结构性解法是 **INT8 量化**（权重从 32/16 位改为 8 位整数：体积约降至四分之一到一半，CPU 上整数运算通常更快，
    代价是召回下降若干个百分点，且需要一批有代表性的校准画面）。量化产物等于一副新眼镜，必须对着**重建后的**
    真值集重跑横评才能采用。偿还：M10 打包前评估（那时关心安装包体积与低配机器，真值集也已重建）。
    4b 顺带确认当前权重是 FP32 还是 FP16——若已是 FP16，量化收益减半
37. `eval-reports/m5-b-t4a2/runs/` 混有 V1 与 V2 两轮的逐配置原始 JSON（V2 中途停止）。汇总 `report.md` 与
    `metrics.json` 是完整 V1，可用；`runs/` 不可用于逐帧复核。偿还：随 TD-33 一并从版本库移除，本地保留

### 6.5 暂不做

- 读游戏内存、注入进程、规避反作弊（永久禁止）；AI 操控竞技网游（永久禁止）
- macOS / Linux；账号系统、云存储、自动更新；语音推理占用 GPU（视觉侧本地 GPU 见 D-39）
- 预先创建空模块空目录占位文件；用户自定义性格提示词
- 截图作为待机话术上下文（隐私，永久禁止）
- 同时解说多个游戏（一次一个，按 [active].game 切换，托盘手动选）
- 适配动态加载与插件市场（端口按可升级形状设计，现阶段 main.py 显式装载）
- 为"AI 代玩"类 post-MVP 做端口设计（操作输出与陪玩解说不通用）
- 对象跟踪 / 分割、视觉 embedding、latent 世界模型进主线（条件重开：B-T11 触发条件）
- 快线输出 JSON 或 DSL（不省时间，需解析与降级路径；深线可结构化）
- 光流自校准视角转角（先看鼠标相对量够不够）
- 初始化或深线联网查剧情类内容（D-40，永久）
- 让模型直接重写整份信念状态或 cognition.md（只许提补丁）
- 各游戏数据层面做不到的事项见各自 md 的「数据边界」

### 6.6 多开发者协作约定（生效中）

1. 适配开发者只允许改 games/<自己的游戏>/、prompts/<自己的游戏>/、data/<自己的游戏>/、docs/<自己的游戏>/ 与自己游戏的 md；主干改动经架构师下发
2. 端口不够用时立即停下来找架构师改端口，不许在适配里绕路
3. 主干与适配只经端口通信，双向都是；test_layering.py 会当场抓违规
4. 端口带版本号；不匹配拒绝装载
