# AGENTS.md
最后更新：M1-T1 下发前（初版）

## 项目概况
目标：一个常驻 Windows 桌面的 AI 电子宠物，在用户玩 CS2 时实时观战、解说、吐槽。

技术栈：
- 后端：Python 3.12 + FastAPI（本地服务，仅绑定 127.0.0.1:8737）
- 前端：Tauri 2.x + TypeScript + Vite，不使用任何 UI 框架
- 语言/视觉模型：阿里云百炼 通义千问 Qwen3-VL 系列，走 OpenAI 兼容协议
- 语音：本地 CPU 神经 TTS（ONNX 格式），严禁占用 GPU
- 截图：Windows Graphics Capture（WGC）
- 打包：PyInstaller（后端）+ Tauri bundler（外壳）
- 精确版本号在 M1-T1 完成后回填

目录结构：（当前只存在 M1-T1 建立的部分；后续任务按需新增，禁止预先创建空模块）
- /backend —— Python 后端工程
- /backend/src/pet —— 后端源码包
- /backend/tests —— 后端测试
- /frontend —— Tauri 桌面应用
- /docs —— 文档

常用命令：（M1-T1 完成后回填实际命令）

## 当前状态
当前里程碑：M1 —— 一个会说话的桌面宠物（完全不懂游戏）

我如何亲眼验证：双击启动 → 桌面出现宠物 → 可拖动、始终置顶 → 热键收起再召回 →
等 30 秒它自己说一句话，气泡、表情、语音同时出现 → 托盘右键退出

已实现：
（暂无）

未实现：
- M2 接入 CS2 官方数据接口，用模板句反应 6 类事件
- M3 接入通义千问，真实吐槽 + 双性格 + 局内记忆 + 花费显示
- M4 打包分发，朋友可安装
- M5 读屏幕击杀提示（全场首杀、被同一人连杀、掉线卡顿）
- M6 外观自定义与心情系统
- M7 跨局长期记忆
- M8 语音对话输入
- M9 AI 玩杀戮尖塔
- M10 AI 玩文明 6

## 约束
禁止修改：
- /AGENTS.md（由架构师维护，coding agent 一律不得改动）
- .gitignore 中与密钥相关的条目
- 任何未在当前任务规格中点名的文件

编码规范：
- Python 全量类型注解；跨模块传递的数据必须是 dataclass 或 pydantic 模型，禁止裸 dict
- 后端服务只绑定 127.0.0.1，禁止监听 0.0.0.0
- 密钥只从配置文件或环境变量读取，禁止出现在源码、测试或提交历史中
- 禁止裸 except 后静默吞异常；捕获必须记录且有明确处理路径
- 每个任务完成后必须 commit 并 push 到远程仓库 main 分支
- 提交信息以任务 ID 开头，例如：M1-T1: 建立前后端工程骨架

技术债：
（暂无）

暂不做：
- 读取游戏内存、注入游戏进程、任何规避反作弊的手段（永久禁止）
- 让 AI 操控 CS2 或任何竞技网游（永久禁止，等同作弊）
- macOS / Linux 支持
- 账号系统、云端存储、自动更新
- 让语音或视觉推理占用 GPU（会导致游戏掉帧）
- 预先创建空模块、空目录、占位文件

## 工程原则
1. 不留向后兼容：过时代码直接删，不加兼容层、不写 fallback。
   （例外：已有真实数据的存储结构变更须先确认，不得静默丢数据。）
2. 选满足当前需求的最简实现。不做预防性抽象，不加没人用的配置层。
3. 先跑通最小端到端版本再往上加。绝不为未完成的功能拆掉已经能跑的东西。
4. 关注点分离，按已存在的职责边界拆模块，不为设想中的未来拆。
5. 优先用成熟且在维护的库。没有明确理由不自己造。
6. 加新依赖或自己写之前，先查项目现有依赖能否解决，并说明查过什么。
7. 难以回退的决策（数据模型、核心接口、技术栈）按长期方案定，不接受"先这样以后再换"；
   易替换的实现按原则 2 从简。
8. 先看成熟产品如何解决同类问题，用已验证的模式，但按本项目规模裁剪，
   不照搬为更大规模设计的方案。
