# AGENTS.md
最后更新：M5-T7.10 验收后（快线冻结、上传宽度 896 定案；M5-B 细化前）

## 文档分层（本次起生效）

- **AGENTS.md（本文件）**：主干、端口、跨游戏共识、流程与工程原则。所有开发者必读。
- **CS2.md**：CS2 适配的全部契约、规则、实测数据与纠错记录。改 `games/cs2/` 前必读。
- **ADAPTER_GUIDE.md**：新游戏适配开发指南。适配开发者的开工文件。
- 每份事实只存在于一个文件中，其他文件用引用，不复制。
- 三份文件 coding agent 一律不得修改（修改权限规则见「约束」）。

## 项目概况

目标：一个常驻 Windows 桌面的 AI 电子宠物，在用户玩游戏时实时观战、解说、吐槽。
主干与游戏解耦：每个游戏的完整理解管线（数据接入 → 事件 → 发言决策 → 事实句）
打包成一个独立适配，经统一端口向主干递「发言请求」。主干只负责嘴（语音与开关）、
钱包（模型调用与花费）、安检（闸门引擎）、脸（前端桥）。
当前已落地的适配：CS2。战争雷霆适配由第二位开发者依 ADAPTER_GUIDE.md 开发中。

技术栈：
- 后端：Python 3.12.x（>=3.12,<3.13）+ FastAPI 0.141.1 + uvicorn 0.52.1
  + pydantic 2.13.4 + websockets 17.0.1 + httpx 0.28.1。仅绑定 127.0.0.1:8737
- 前端：Tauri 2.11.5 + TypeScript 7.0.2 + Vite 8.2.1 + Prettier 3.9.6，不使用 UI 框架
- 构建环境：Node.js 24.x、npm 11.x、Rust 1.97.x（MSVC）
- 语音：Windows OneCore 系统语音，PowerShell 合成 WAV + WinMM 直播放，
  可从任意线程立即中断，零模型零 GPU。神经网络语音是 M7，尚未开始
- 语言模型：经 OpenRouter 调用（OpenAI 兼容协议；产品负责人在北美，无法注册
  阿里云百炼，不走厂商直连）。**任何代码与文档不得写死型号名或上游服务商名**，
  一律作为配置传入；各游戏可在 [llm.profiles.<name>] 定义自己的模型档位
- 截图：Windows Graphics Capture（尚未接入，M5）
- 打包：PyInstaller + Tauri bundler（尚未启用，bundle.active = false）

目录结构：
- /backend/src/pet/
    main.py                  读配置、装载当前适配、挂路由、生命周期
    core/                    主干，游戏无关（分层铁律由 tests/test_layering.py 强制）
        adapter_api.py       端口 v1：SpeechRequest / GameStatus / GameAdapter /
                             CoreServices（见「核心契约」）
        speaker.py           发言执行：串行队列、模型调用、回退、花费、模式锁定
        gate.py              闸门引擎：按 vocabulary_id 解析词表绑定表为运行时闸门
        capture.py           截屏与画面变化检测（游戏无关基础设施，M5-T1/T3）：
                             WGC 精确窗口抓帧、块级检测器、全量录制、离线重放、
                             前台命令行探针。零模型、零网络
        config.py  network.py  bridge.py  speech.py  lines.py  llm.py  prompt.py
    games/<game_id>/         每游戏一个适配包，含完整管线与自己的 eval/ 评测工具
        （CS2 的模块清单见 CS2.md）
    games/generic/           通用视觉模式（M5）。**当前只有 eval/**：视觉考卷工具、
                             机械区域生成、端点与地理对照跑卷。
                             adapter.py 尚不存在，未注册进 built_in_adapters
- /backend/tests             后端测试与 fixtures；test_layering.py 是分层警报器
- /backend/prompts/
    personality.md           共用性格层，**产品负责人可直接编辑，重启生效**
    generic/                 通用视觉的观察提示词：observation-fast.md（快线，一句话）
                             与 observation-deep.md（深线，详述）。跨游戏通用，
                             不得出现任何游戏专属内容
    <game_id>/               每游戏的词库、闸门需求（gate-requirements.json）等
- /backend/data/<game_id>/   每游戏的场景与评测资产（回归资产，不得删除）
- /backend/eval-reports/     评测输出目的地，已 gitignore，产物不进仓库
- /backend/audit/            M4-a 的模块清单（历史参考）
- /backend/config.toml       默认配置；config.local.toml 本地覆盖（已忽略）
- /backend/recordings/       原始录制（已忽略）
- /frontend                  Vite + TypeScript 前端；/frontend/src-tauri Tauri 外壳
- /docs/<game_id>/           每游戏的文档（数据接口能力、社群黑话等）

常用命令（Windows PowerShell）：
- 后端安装：cd backend && python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
- 后端运行：.venv\Scripts\python -m pet.main
- 后端测试：.venv\Scripts\python -m pytest
- CS2 评测工具入口：python -m pet.games.cs2.eval.<replay|bench|scenario_synth|...>
  （各工具用途见 CS2.md）
- 前端安装 / 运行 / 构建：cd frontend && npm.cmd install /
  npm.cmd run tauri dev（需先启动后端）/ npm.cmd run build 与 format:check

约定：
- Windows PowerShell 中前端命令一律 npm.cmd；python 不可直呼时用 py -3.12
- 后端与前端由人手动分别启动，打包（M10）之前不做自动拉起
- 模型 API 密钥只从环境变量 OPENROUTER_API_KEY 读取，绝不入库

## 核心契约

改动前必须先与架构师确认。以下定义与代码实现必须逐字段一致。

端口 v1（core/adapter_api.py，PORT_VERSION = 1）：

    SpeechRequest —— 适配向主干递的唯一「发言条子」：
        request_id, game_id, fact_text（事实句）, urgency（0–100）,
        interrupt, supersedes_request_id（二者本版只入库不实现差异化行为，
        当前等价于一律 interrupt=True；语义留给需要的适配提出后实现）,
        vocabulary_id（闸门与提示词用哪份游戏词表，None=不加）,
        llm_profile（模型档位名，None=默认 [llm]）,
        fallback_text / fallback_emotion（模型失败时的模板兜底）, ts

    GameStatus —— 适配向主干报的游戏状态：
        game_id, state（适配自定粗粒度状态）,
        summary: dict（展示字段，主干不解释语义，前端与托盘泛化显示）

    GameAdapter（Protocol）——
        adapter_id, display_name, port_version（装载时必须等于 PORT_VERSION，
        不等则拒绝装载并记 ERROR）, http_router | None,
        async start(core), async stop()

    CoreServices —— 适配对主干的全部认知，仅此五个回调：
        submit_speech(SpeechRequest), publish_status(GameStatus),
        can_submit_speech() -> bool（是否有前端消费者，供适配决定是否扣配额）,
        speech_is_muted() -> bool,
        reset_speech_session()（对局边界重置连续失败锁定）

分层铁律（tests/test_layering.py 静态强制，违者测试失败并指出文件与行号）：
    a. core 不得 import games 下任何东西
    b. 游戏包之间不得互相 import
    c. 游戏包的生产模块不得 import 本游戏的 eval/
    d. pet/ 顶层只允许 main.py（及 __init__.py）
    e. 游戏包对 core 的 import 只允许 adapter_api、config、llm、prompt
       四个模块（eval/ 工具额外允许 gate）

主干对发言条子的三项裁决（宠物的生理，不是游戏逻辑，永远留在主干）：
    1. 开关永远有效——静音时任何适配的条子都不播
    2. 只有一张嘴——同一时刻只播一句，打断规则统一执行
    3. 频率保险丝——粗粒度全局每分钟上限（宽松到正常适配碰不到），
       防止写坏的适配把宠物变成机关枪。【注：本条为既定设计，实现排在
       战争雷霆适配接入前，见技术债 3】

speaker.py 的既定行为（自 M3 起不变）：
    请求串行队列先进先出；一次事件最多一次模型调用绝不重试；
    失败 / 空输出 / 闸门命中一律回退 fallback_text；
    连续失败 3 次锁定本局模板模式；花费统计与托盘显示。

提示词拼装（core/prompt.py）：
    系统提示 = prompts/personality.md + prompts/<vocabulary_id>/vocabulary.md（若有）。
    性格层共用、游戏知识层随适配，是既定分层决策。
    词库整份注入不做筛选（输入几乎免费，见「关键实测数据」）。

闸门引擎（core/gate.py）：
    按 vocabulary_id 读取 prompts/<id>/ 下的词表绑定表与 gate-requirements.json，
    解析为运行时闸门；解析失败整表失效并回退保守规则。
    改词库不需要改代码。

WebSocket ws://127.0.0.1:8737/ws：
    后端 → 前端：
      {"type":"utterance","id":...,"text":...,"emotion":...}
      {"type":"state","speech_enabled":bool,"muted":bool,
       "game":{"game_id":...,"state":...,"summary":{...}}}
    前端 → 后端：
      {"type":"request_idle_line"} {"type":"set_speech_enabled","value":bool}
      {"type":"set_muted","value":bool}
    规则：连接建立先发 state 再发问候；开关变化广播 state；game 字段任何时候
    必须存在，未知子字段为 null；非法消息只记 WARNING 不断连；
    Origin 存在但不在白名单则拒绝，缺失则允许。

配置文件段落（backend/config.toml）：
    [active]  game = "cs2"
    [speech] [idle] [llm]（顶层，主干所有）
    [llm.profiles.<name>]  可选模型档位覆盖段
    [games.<game_id>.*]    各游戏自己的配置段（CS2 的段落见 CS2.md）
    档位可选字段：base_url（该档位专用的 OpenAI 兼容端点，缺省用全局）、
    api_key_env（该档位密钥所在的环境变量名，缺省 OPENROUTER_API_KEY）。
    **密钥值本身永远不进配置文件**；指定的环境变量缺失时必须明确报错，
    不得回退到其他变量或默认端点。仅默认端点发送服务商特有的路由参数。

Utterance（core/lines.py）：id 非空, text 非空,
    emotion: neutral / happy / angry / surprised / speechless

HTTP 路由：适配经 http_router 挂载自己的接收端点（CS2 的 /gsi 规则见 CS2.md）。

## 当前状态

当前里程碑：**M5-A 收官冻结，M5-B「跟得上」细化中。** 快线合同经 T7.9 定稿并在
T8 双模型矩阵验收（现任 Qwen 3.7 Flash 冻结回归真值 5/5；外部候选 2/5 但
1080p 下读数字明显更强、TTFT 中位仅 500–760ms，保留为常设对照）；
上传宽度曲线证明 896/1280/1920 读数率持平（10.9/10.1/11.3%）而 1920 多付
0.5 秒 TTFT 与 85% 费用——**生产上传宽度 896 定案，瓶颈在模型内部**；
录制宽度已改 1920 默认。T7.10 OCR 探针：WinRT 中文可用、单帧中位 35–76ms、
640 素材零输出（分辨率是硬门槛）；两遍式实验：首遍留出召回 85.2%（58ms），
+彩色二次 96.3%（247ms）；灰度二次与双通路无增益。
四段 1080p 片段答案键（会话背景 + 片段现场记录 + 判卷专名表，
专名表与模型上下文结构隔离）已入库。
进行中：选择性二次 OCR 原型（持久工作进程 + 缓存差分 + 预算上限）；
随后 M5-B 细化：深线 + 两栏笔记本 + 快线背景槽注入（OCR 差分为第三路供货）。
排队：T7.8（429 冷却）、【推测】段泛滥的一句话收紧（下次碰提示词时）。
快线不做推理已定案：现任模型推理为开关无档位、开启延迟不可接受（T7.11 销案）。
视觉美国区端点仍等服务商账号人工审核（技术债 20）。

已实现：
- **M1 桌面宠物**：置顶悬浮窗、拖动、显隐热键、托盘菜单、表情、气泡、
  系统语音（可中断）、WebSocket、待机播报
- **M2 看得懂 CS2**：GSI 接入与自动安装、录制回放、会话与主体识别、
  事件检测、发言策略、模板话术、对局生命周期
- **M3 每句话由大模型当场生成**：代码产事实句 + 模型只管文风、词库与闸门、
  线上接入与回退、评测基建、连杀收敛。产品负责人真实对局验收
- **M4 多游戏地基**：删 4.4MB 过程产物并产出模块清单；端口 v1 定稿；
  一次性重构落地——core / games/cs2 / eval 三层分离、全目录英文化、
  性格与游戏知识分层、每游戏独立配置段与模型档位、分层警报器上线。
  迁移前后：35 场景选中序列逐字节一致、提示词逐字节一致、通过数 414>409

未实现（滚动细化，只细化当前里程碑；主线与适配线并行）：
- **M5 通用视觉**（默认关闭）。架构上是主干侧能力：通用模式相当于一个
  "用屏幕当数据源"的特殊适配，经既有端口产出发言请求。**已按产品负责人决定
  拆成三段，且只含通用模式**——CS2 视觉适配与战争雷霆视觉归适配线，不阻塞主线：
    M5-A 看得见：截屏 + 快线观察 + 窗口标题查表识别游戏 + 滚动观察日志。
      验收物是**那份观察日志**（读它十分钟内看见了什么），不是听宠物说话
    M5-B 跟得上：两栏笔记本 + 慢线深读 + 知识卡。验收物是**笔记本**
    M5-C 开口：发言决策 + 接端口 + 说话。届时说话链路早已就绪，只写"何时开口"
- **M6 跨局长期记忆**：按游戏分仓；通用模式下即"让笔记本睡一觉不丢"
- **M7 神经网络语音**：必须保留可随时中断，延迟不得明显劣于系统语音
- **M8 语音对话输入**
- **M9 外观自定义、心情系统与设置面板**（设置面板：性格 / 外观 / 适配管理的 UI 壳）
- **M10 打包分发，朋友可安装 —— MVP 完成线**

适配线（第二位开发者，代码审查归架构师）：
- 战争雷霆实时数据适配：**现在可开工**，开工文件 ADAPTER_GUIDE.md。
  对标 CS2 的 M2+M3 深度；数据源为游戏自带 localhost:8111 本地接口
- 战争雷霆视觉适配：M5 之后
- CS2 视觉适配（OCR + 地图识别等）：M5 之后，落在 games/cs2 内
- CS2 地图战术建议（原 M3-T14）：随时可做，落在 games/cs2 内，触发于回合开始，
  可预生成。CS2 待办清单见 CS2.md

Post-MVP：适配形态升级为独立分发（A→B→C）、设置面板的适配下载页、
AI 玩杀戮尖塔、AI 玩文明 6（"AI 代玩"与本产品端口不通用，不预留设计）。

多开发者协作约定（生效中）：
1. 适配开发者只允许改 games/<自己的游戏>/、prompts/<自己的游戏>/、
   data/<自己的游戏>/、docs/<自己的游戏>/ 与自己游戏的 md；主干改动经架构师下发
2. 端口不够用时**立即停下来找架构师改端口**，不许在适配里绕路——
   现在开发者只有两人，端口还能免费改，这个窗口要用满
3. 主干与适配只经端口通信，双向都是；test_layering.py 会当场抓违规
4. 端口带版本号；不匹配拒绝装载

## 关键设计决策（仍在生效，不要重新讨论）

**确定性的事实用代码算，需要品味的表达用大模型生成。** 事件判定、局势累计、
事实句全部由代码确定性产出；模型只负责"把事实句用网友口气重说一遍"。
此分工跨游戏通用，所有适配必须遵循同一形状。

**代码写平实事实，不写社群黑话；不使用内部档位名。** 让代码写黑话等于把品味
判断收回代码；写档位名等于造一层需要模型解码的编码。跨游戏通用。

**发言策略属于适配，不属于主干。** 每个游戏的节奏（CS2 事件间隔 2–3 秒、
战雷导弹飞行十几秒）差异太大，任何"通用策略"都是把一个游戏的实测
当成所有游戏的规律。CS2 磨出来的策略机制作为可选工具箱保留，不强加。

**性格与游戏知识分层。** 性格层（怎么说话）共用一份、产品负责人直接编辑；
游戏梗知识（说什么内行话）随适配走。拼装由主干完成。

**模型的事实准确率不是评测指标。** 事实由代码保证；要检验的是文风好不好听，
只能由人肉眼判断。三件事分开：事实准不准（代码 + 单元测试）、
会不会漏说（人耳）、会不会编造（运行时闸门）。

**模板路径永久保留**，模型失败时回退，宠物永不哑火。不设花费熔断，只做显示。

**回合/段落结束不发言**，槽位留给视觉层之后的长记忆反馈。

**不做交火期禁言**：及时的情绪反馈本身就是卖点。

**通用视觉分快慢两条线，它们的成本性质相反。** 实测：输入几乎免费、输出串行昂贵，
同一次请求无法兼得"快"与"深"。
  快线（热路径）：每个上传帧都跑，发言链路在等，预算 ≤1 秒，一句话硬顶 max_tokens=80，
    不联网。产出进滚动时间线
  深线（冷路径）：只挑少数帧、被触发才跑，无人等待，不限长度，可联网（受两货架规则约束）。
    产出进笔记本，标注"深读"
  两者以"见闻"身份汇入同一本笔记，由笔记员（强推理档）合成"认知"——不需要额外的合并机制。
  触发深线的只有三种：快线自己举手、笔记员点名提问、闲时补课。
  **再多的深度需求都表达为"笔记员的提问"，不加第三条线。**

**快线的上下文形态永远是单图，且只放分钟级稳定或机械生成的信息（修订定案）。**
判据：并发使一切秒级更新的上下文必然过期。清单：当前帧 + 游戏名 +
区域提示（含真实基线秒差）+ 键位输入窗口（机械聚合）+ 将来的笔记摘要/知识卡
（分钟级更新，免疫异步滞后）。**文字帧时间线已从快线输入移除，只喂推理层**——
它是秒级信息，并发下必然过期，且实测为复读抄袭的病灶（模型逐字抄上下文，
含在连续 429 丢弃后抄出"较上一帧"句）。不做多帧并送。

**快线输出合同（T7.9 版）：模型只写散文，数字全部机械注入。**
模型输出【全局画面】（一到两句：画面上正在发生什么）+【局部】（仅当提供聚焦
区域时：该区域及紧邻环境正在发生什么、它是什么）。允许复读——重复即"场景稳定"
的证据，去重归笔记员。回溯禁令不变（现在进行时合法，"出现了/比之前"非法）。
日志渲染器机械注入三指标（全局像素变化、区域占比、区域强度——三者管线本就在算）
与【玩家输入】行；模型一个数都不写，无从编造。
聚焦区域以外接框方位词+占比表述（"画面中央偏左、约 30% 的区域"），
**格子编号不再发送给模型**——坐标泄漏在结构上绝根。区域占比 >0.5 不提供聚焦
（"占比高=镜头动"已被 T5a 分布重叠数据否证，不设大范围语义标签）；
心跳帧注明定时快照。字数为软引导（每段一到两句），max_tokens=200 只作保险丝。
md 正文时间用 T+秒（会话内记忆无需墙钟精度），头部留墙钟锚点；
**md 是推理层的食粮，jsonl 是司法存档**，两者分头优化。

**事实优先级阶梯（T7.5 实测有效）。** 可读文字与数字 > 对象出现/消失/移动 >
界面结构 > 纯光效（须以"仅"开头四到六字自标记，供推理层一键降权）。
杀戮尖塔含数字占比 6.4%→34.3% 验证方向；统计口径须排除系统注入符号
（坐标假数字曾把灰区"含数字"污染到 26.7%）。

**快线不做深思，外部机制替它卸负。** 区域框、三指标、优先级阶梯、键位摘要、
将来的知识卡——外部机制每强一分，模型需要的内部推理就少一分。
快线的理想态是"看一眼就说对"；轻推理仅作为单变量 A/B 实验（T7.11），
若开启须知代价是单条落地 4–5 秒，M5-C 前重新评估。

**拒绝有状态记忆模型方案。** 四个理由：与并发数学不相容（有状态要求按序）；
丢帧即失同步且模型不自知；图像 token 缓存支持参差不可赌；
违反"记忆是文字"（文字记忆便宜、可审查、可迁移、可被笔记员纠错）。
提示词缓存作为正交小优化保留（前缀顺序：稳定在前）。

**键位输入进快线：机械事实，不做语义翻译。** 格式如
"W 按住 2.8 秒；左键点击 5 次（间隔约 0.15 秒）；鼠标向右大幅转动"；
"W=前进"由模型结合游戏名自解（键位表属知识卡，M5-B）。
**结构隔离**：生产适配自带动作键白名单监听器，与探针全键盘模式在代码路径上
分离——模型管道里全键盘数据在结构上不存在（技术债 23 前半已偿）。

**时间线的更新频率靠并发，不靠单次变快。** 实测单次往返已接近管道物理下限，
每秒发一帧、允许数个请求同时在飞、按时间戳排序入库即可。
串行队列是"嘴"的规矩，不是"眼睛"的。

**记忆是文字，不是图片。** 截图即读即弃（调试落盘除外），留下的只有观察文本与笔记。

**笔记本分两栏，垃圾可以进来但住不下。**
  见闻栏＝证词（可能错、允许错、滚动淘汰）；认知栏＝判决（须被反复印证才能升格）。
  笔记员每几分钟做三件事：升格、驳回、翻案。**把关的时点是写入记忆那一刻**，
  不是每一帧——错误观察最多毁一句吐槽，被写进记忆才是灾难。
  通用模式没有真值来源（不同于 CS2 的 GSI），目标是**会自我纠错**，不是永不出错。

**游戏身份由窗口标题与进程名确定性获得，不是模型的推理任务。**
准确率 100%、成本 0、延迟 0。让模型去猜是设计倒退。

**两货架规则（游戏知识的边界）。** 分界线是"玩前可得 vs 玩后所见"：
  货架一·开箱即知（可 web search，每游戏一次，本地缓存）：类型、世界观一句话、
    操作与 UI 惯例、机制体系、社区通用术语、默认键位表。**机制可以查到饱**
  货架二·亲眼所见（只能从观察与推理积累）：剧情、角色命运、结局、地图与世界内容
  游玩中允许**由观察触发的按需查询**：屏幕上出现了不认识的名词才去查那个词条，
    按词条缓存、限频。触发条件必须是"亲眼在屏幕上见过"。
  剧透按产品负责人决定**不做硬限制**，只在提示词中 discourage。

**评测工具的题干必须与生产输入同构。** 出题人的先验知识不得以任何形式进入
模型可见的内容——不得有题目专属提示词、不得把题号或类别送进消息体、
区域提示必须由检测器机械生成（零语义的格子坐标），不得人工撰写语义指路。
本项目曾因此使抗幻觉题的答案被印在题干上而整题作废。

**检测器参数选型用「带约束筛选」，不用字典序排序。** 纯「漏帧优先」的排序会
一路推到 k=0.8/margin=0 的「全都传」角落（纯静止画面也传 62%，等于检测器关闭）。
正确做法：硬约束（字幕/UI 剧变零漏检、过渡段漏检个位数、世界事件命中不垫底）
之内最小化真阴性锚点（B-idle 与 A-static）的上传率；燕云类「零输入动态世界」
只作成本列不参与优化。约束一加，最优点回到网格内部。

**通用检测器的职责是去重，不是侦察。** 「准心里的远处小目标」在块平均度量下
物理上不可分（5% 像素 × 大变化被稀释成 5/255），可靠检测此类目标是游戏特化
视觉适配的职责。缓解手段：固定基线的累积效应（晚发现而非发现不了）、
60 秒心跳、以及中心加权 margin（技术债 24）。

**帧选取不做时间持久性门控（P=1 定案，不要重新讨论）。** 1 秒采样下真实事件
（对白弹出、UI 切换、转视角）多数只在一帧里表现为剧变，「连续两轮才算数」
把它们成批滤掉（实测漏检十倍、屏幕六成格子变化被判 no_change、
探索录像连续 5 秒零上传）。滤闪烁由块级自适应噪声地板独立承担。
**教训：门控的时间尺度必须与目标事件的时间尺度比对后再设。**

**camera_motion 规则已删除。** 「镜头移动」在像素空间不是可分类别：转向与非转向
轮次的块占比分布严重重叠（中位 13.2% vs 4.9%，P75 22.6% vs 19.3%，
非转向最大 97.9%）。其两项作用由既有机制承担：大面积变化在 P=1 下自然走
persistent_change 上传；整屏变化时的提示清空由稀疏抑制承担。

**上传与区域提示是两个独立决策，不得重新耦合。** 是否上传只看有无 confirmed 块；
是否附区域提示只看 confirmed 占比是否 <= region_sparsity_max（当前 0.25，
待与扫描统一）。超过上限时清空提示但照常上传。

**两条时间线（画面 / 输入）用同一个单调时钟；帧时间尽量取内容时刻。**
键鼠事件几乎零延迟，画面天生滞后（合成 + 缓冲 + 轮询量化），实测两者错位约一轮
（真值后移一帧使召回 29%→38.7%）。修法三层：统一 perf_counter、
帧时间戳取 WGC 呈现时刻（若 zbl 暴露）、互相关校准兜底（技术债）。
发给模型的场景对精度宽容（错一两百毫秒无感），校准真值才吃精度。

**输入遥测的隐私设计已定档（实现暂缓，见技术债 23）。** 两档白名单：
动作键（通用基础键 + 当前游戏键位表声明的键，逐事件记录）；
其余可打印键只记窗口内计数，不记身份与顺序——无身份无顺序的计数在数学上
无法重建文本。另加打字风暴静默与输入法合成静默两道纵深。
发给模型的永远是抽象信号，不是键码流。
**开发阶段临时开启全键盘记录是产品负责人的显式决定**（保证校准与建键位表的
上下文完整），仅限本机、仅进被忽略目录，且提交进仓库的任何报告不得包含
动作键集合之外的逐键数据（只允许「其他键合计」）。

CS2 专属的设计决策与产品事实（事实句规则、连杀收敛、输出字数上限、
纠错记录等）已全部移至 **CS2.md**。

## 反复出现的失误模式（引以为戒）

这些是本项目实际发生过的错误，同类形状出现过多次。

**把未验证的分析当成诊断。** 曾按回合号分组统计而录制含三局比赛，
虚构出不存在的缺陷并施工一轮。规则：从数据得出结论前先核对数据的组织结构。

**声称测试失败是"既有问题"却不复现。** 曾把自己引入的回归报成"非本轮引入"。
规则：任何此类声称必须在上一个提交实测复现并附输出，否则按本轮引入处理。
"我没改那个文件"不构成理由。

**在既有时序机制前后插入新等待，却不重算时间预算。** 曾因此把释放窗口
压成 2.5 秒宽导致整句丢失。规则：新增等待/延迟/收敛时重推所有相关时间预算。

**删除清单靠印象，不先查引用。** M4-a 曾把测试依赖的 5 份回归资产当垃圾删除。
规则：删除任何目录前先全仓库检索引用，逐个归类后再定清单。

**用自己临时想的关键词自证"没问题"。** 规则：先定清单，再拿它去扫，不得反过来。

**验证环境不干净。** 规则：验证前确认 `python -c "import pet; print(pet.__file__)"`
指向本次克隆。

**凭直觉给阈值。** 规则：规格中任何阈值必须附实测依据，或明写"此数待实测确定"。

**重命名标识符时漏改引用方；同一份清单存两处抄本。** 规则：重命名被提示词或
数据文件引用的标识符必须同批同步；两处清单必须有测试断言其一致。

**加新机制前不查既有机制。** 规则：先说明查过什么、为何现有机制不够。

**规则与内容自相矛盾 / 自查范围不明。** 规则：自查方法必须写明匹配范围。

**需要人工判读的指标被自动统计代替。** 规则：把"逐项给出判定与理由"写成硬性验收项。

**底层行为靠读代码推断。** 规则：涉及系统 API / GPU / 网络 / 外部服务时，
规格写成"按 a→b→c 顺序尝试，每步测量，够用就停"。

**架构师连续用"等定型了一起写"推迟 AGENTS.md 更新。** M4 关闭后连续 14 个任务
未更新本文件，比此前两次事故都长；每一轮的推迟理由单看都成立，累积起来
就是本条失误模式本身。**规则：无论是否"定型"，每三个任务必须更新一次本文件；
判断标准不是内容是否稳定，而是距上次更新过了几个任务。**

**评测的题干泄露了答案。** 规格中批准了"题目专属提示词"这一字段，
于是抗幻觉题的题干里写着"若无显著事件则必须返回空"，整题作废。
规则：评测输入必须与生产输入同构，出题人的先验不得进入模型可见内容。

**采购了条款禁止本用途的服务套餐。** 某订阅套餐明写"仅限编程工具中交互式使用，
禁止用于自动化脚本与应用后端"，而本项目正是应用后端。
**规则：本项目是自动化后端，任何"仅限交互式使用"的套餐一律不可用，
购买任何额度前先读用途条款。**

**通用提示词的示例走私了具体游戏内容。** T7.3 修订在示例里写了"闪电、手电筒"
等具体机制名词，等于告诉模型往这类事件上靠，且对照重跑真值回归 2/5。
**规则：示例也是提示词内容；通用提示词的示例不得含任何具体游戏的机制、道具、
事件名词，只允许"某界面元素出现/某区域亮度变化"这一抽象层级。**
与"评测题干不得泄题"同族。

**为滤噪声加的时间门控把真实事件成批滤掉。** 「连续两轮才算数」在 1 秒采样下
恰好滤掉只持续一帧的真实剧变（对白弹出、转视角），漏检十倍。
**规则：任何门控 / 滤波 / 收敛窗口，设立前必须先量目标事件的持续时长，
确认事件能活过这道门。**

## 关键实测数据（主干；CS2 专属数据见 CS2.md）

**前端**：release 空闲 4.19% 单核、隐藏 1.12%；呼吸动画外置 + 8Hz 较优化前降 88.9%
（单纯提升合成层反而更差）。静态约 408 MiB / 7 个 WebView2 进程，为固定成本。

**后端**：启动最坏 0.85 秒；稳定 50 MiB；30 分钟无泄漏；
/gsi 中位 0.448ms、P95 0.670ms（接入模型后未退化）。

**语音**：20 字中文出声延迟最坏 0.42 秒；停止 1.01 秒内返回。
`speak()` 先 `stop()`——**新句子掐掉正在播的那句，不是排队**（M3-T12 实测确认）。
一句 19–30 汉字约 2–3.5 秒说完；加上模型 0.5–1.5 秒，
**一句话从选中到说完约占 4 秒**——任何发言策略都要对着这本物理账设计。

**截屏与画面变化检测**（M5-T1/T1.5/T3 实测）：
- 后端停在第一顺位 Windows Graphics Capture（zbl 0.7.1），按精确 HWND 抓单窗口
- 抓帧最大 38 毫秒；六指标含块级统计合计中位 9.4 毫秒（1920x1080）
- 1 秒轮询单核 CPU 约 3.3%，2 秒档约 2.1%
- **单一平均振幅指标不可用**：环境动效（雨、浪、植被）与局部文字变化的失效方向
  相反，不存在同时满足两者的阈值。现改为块级自适应噪声地板 + 时间持久性
- **必须容忍数秒级的空帧窗口**：曾观测到击杀后 5.86 秒才有下一帧；
  WGC 无新帧时必须非阻塞取帧，阻塞式会把探针锁死 49–90 秒
- 已知兼容性债：某游戏在 WGC 会话存续期间游戏内光标消失，跨游戏负对照未复现，
  责任边界只能写成"该游戏与当前捕获会话的交互异常"，不写特判
- **P=2 的漏帧证据**（M5-T4/T5a，1403 帧真实录像）：P 从 1 提到 2，局部剧变漏检
  11.6→115.9、输入活动漏检 49.0→153.3；存在门控前 65.97% 块变化被判 no_change
  的轮次；探索录像连续 5.0 秒零上传。T5b 修正后四组录像 no_change 全线大降
  （探索会话 200→66），上传率 46–58%（约 $0.1/小时），
  简化后判定合计中位 4.7 毫秒
- 输入与画面时间线错位约一个轮询周期（真值后移一帧使镜头移动召回 29%→38.7%），
  对齐修正见「关键设计决策」
- **第二轮网格（108 组 × 16 角色，1728 行）与参数定案依据**：定案组合对比现值——
  字幕剧变漏检 3→0、总漏检 37→4（残余全在卡牌会话）、世界事件命中 14→19、
  暴雨静止上传率同为 2.0%（k 从 2.5 降到 1.0 地板全程稳住）、
  动态会话成本 $0.24→$0.28/小时。三个入围组合 margin 全部 =4/255：
  安静块的唯一防线是加数不是乘数（中位数近零时 k 乘不出门槛）
- 燕云十六声「纯静止」样本经逐帧复核重定性为「零输入动态世界」：
  光照转变段 23 帧 21 帧真实触发、字幕更新属应抓事件——40.99% 不是误报率。
  **真阴性锚点只剩 B-idle（灰区暴雨静止）与会话 A 的静止段**
- 互相关滞后测量：四个过渡段测出 2.1/7.0/2.1/23.1 秒、相关口径含负值——
  过渡段不是干净阶跃，当前素材测不出可信常数。技术债 22 降级为按需：
  真需要真值级对齐时录专门的阶跃实验（静止中只切手电 × 10 次）

**视觉模型**（M5-T2 系列实测，11 题考卷 × 多模型）：
- **TTFT ≈ 1324 毫秒固定开销 + 0.97 毫秒/token**，三个不同规模的模型在同一
  上传宽度下 TTFT 几乎相同（896 档：2061 / 2078 / 2077 毫秒）——
  **该固定开销来自管道而非模型计算，换模型救不了它**
- 推论：**上下文每多 1000 token 就多等 1 秒**；提示词组装必须"稳定前缀在前、
  变化部分在后"，否则前缀缓存吃不上
- 压缩输出文本对提速几乎无用（快线可见输出仅约 30 token，解码占比很小）；
  其价值在于让滚动时间线可读，属记忆质量优化而非提速
- 上传宽度是唯一有效的提速杠杆：1280→640 砍掉约 1 秒，但 **640 会读错文字，
  896 是质量下限**。原生分辨率在快线出局（中位 9.4 秒）
- 地理位置是最大的单项：亚洲上游 TTFT 中位 2.71 秒，美国上游 1.27 秒，差 1.44 秒。
  但所测美国 serverless 有冷启动长尾（P90 18.8 秒、最大 77.8 秒），不可用于每秒一帧
- **必须锁定单一上游**：未锁定时最大延迟 304 秒与 636 秒，锁定后降至 6.09 秒。
  与 M3 期文字模型同一个病
- 推理 token 会计入输出预算：某模型强制开启推理，60 token 档 22/22 截断、
  8 次可见输出为空。**这是配置问题，绝不能记成模型能力不足**——
  跑卷工具须自动检测截断率并以宽松额度重跑、显著标注"该档不可比"
- 整卷四模型 132 次调用总花费 $0.49；便宜档单帧约万分之一美元。
  **成本不再是本项目的设计约束**——阈值可以往低了定，宁可多传

**语言模型**（当前 CS2 档位锁定单一上游）：
- 事件 P95 约 0.8 秒；约 ¥0.5/小时；长尾可达 15.7 秒——线上必须配短超时与模板回退
- **输入几乎免费，输出是延迟大头**：多 1200 输入 token 仅 +0.119 秒，
  多 87 输出 token +2.234 秒。提示词想写多长写多长，生产坚持单行输出
- 该模型必须显式传 `reasoning_effort="none"`（`low` 会输出英文推理并耗尽预算）
- **固定种子会让温度失效**：多样性采样不传种子；正式评测保留种子以复现
- 上游不锁定则延迟不可比（OpenRouter 单次运行出现过八家分发）

## 约束

AGENTS.md / CS2.md / ADAPTER_GUIDE.md 的修改权限：
coding agent **默认不得修改这三份文件的任何内容**，只负责在提交中带上。
唯一例外须同时满足：产品负责人明确打字同意；任务规格附精确的旧文本→新文本
替换组；agent 只执行替换一个字不得自行增删；任何一组无法逐字匹配立即停止报告；
完毕后报告新的第二行与总行数。设立理由：本项目曾两次因文档未正确更新而使
coding agent 连续九个任务读到过期内容。架构师每轮验收第一个动作仍是拉仓库逐字 diff。

禁止修改（coding agent 不得改动内容，但需在提交中带上）：
- /docs 下全部文件
- /backend/data/<game_id>/ 下全部场景与评测资产（回归资产；测试与实现不一致时
  改实现，不许改资产迁就）
- /backend/prompts/ 下的措辞（代码只负责加载与拼接）

编码规范：
- Python 全量类型注解；跨模块数据必须 dataclass 或 pydantic，禁止裸 dict
- 后端只绑定 127.0.0.1；端口 8737 前后端各只允许一处常量
- 密钥只从配置或环境变量读取；模型型号与上游一律配置传入，禁止源码写死
- **任何入库产物不得包含真实玩家身份**（player.name / 各 steamid 及
  previously/added 对应项一律占位符替换；武器名地图名程序名不属身份信息，
  不得误脱敏）。仓库是公开的
- 禁止裸 except 静默吞异常；网络测试必须注入假客户端不得真联网
- 读取玩家数据段前必须确认数据主体是本人（各游戏的主体识别规则见其 md）
- Tauri v2 权限默认全关，新增能力在 capabilities 显式最小授予
- 窗口尺寸位置一律逻辑像素（DIP）按 scale_factor 换算，禁止硬编码像素补偿
- 前端 Prettier 统一格式化；Rust 过 cargo fmt --check；
  前端视觉组件独立模块，外部不得直接访问其内部 DOM
- 偏离本文件技术栈或分层职责必须在完成报告显式标出
- **提交进仓库的测试不得依赖未提交的机器本地数据**：所引用的本地文件缺失时
  必须以明确理由 skip，不得 fail。本地数据只能让测试跳过，不能让它变红
- 截屏、录制、输入遥测等隐私相关模块不得 import 任何网络模块，
  产物只写入已被 .gitignore 覆盖的会话目录
- 每任务完成必须 commit 并 push；提交信息以任务 ID 开头

技术债：
1. `prompts/cs2/gate-requirements.json` 与 CS2 代码中的事件名/场景标签是两份
   无测试同步的清单。偿还：下一个 CS2 任务顺带加断言测试（CS2 测试可同时读两边）
2. 各游戏的配置模型仍定义在 core/config.py。偿还：战雷适配开工任务中把
   配置模型所有权移交各游戏包
3. 频率保险丝（主干三裁决之三）已定设计未实现。偿还：战雷适配接入主干之前
4. 语句多样性不足、词库利用率待提升。偿还：CS2 适配内做，时机自定
5. `games/cs2/eval/fact_sentence_audit.py` 与 `template_rules.py` 的脏字单字项
   会命中常用词（「操作」命中「操」）。偿还：下次动 CS2 闸门数据时
6. speaker 的 30 汉字输出硬上限比产品目标 19 字长 50%，不得因沿用成为事实标准。
   偿还：M7 神经语音时用语音时长实测定
7. Steam appmanifest 的 installdir 未做路径归属校验。偿还：M10 打包前
8. test_idle_broadcast 仍真实等待且用 model_construct 绕过校验。偿还：M10
9. speech.py 的 PowerShell -EncodedCommand 组合可能被杀软拦截；每次朗读新建
   进程是否改常驻待实测。偿还：M10 打包前验证；常驻化并入 M7
10. requirements.txt 未分运行/测试依赖（测试链 13.9 MiB）。偿还：M10
11. 跨域白名单只覆盖开发来源，打包后来源未确认。偿还：M10 打包前必须复核
12. 无 LICENSE 且 pyproject 未声明（依赖许可已核实无传染项）。偿还：M10 打包前
13. pet.ts 同时承担渲染与拖动发起。偿还：M9 更换外观时
14. 前端与 Rust 测试覆盖薄。观察中，出现回归再引入最小手段
15. OneCore 语音测试在无中文语音的机器上失败/跳过，环境问题或测试设计待确认。
    偿还：M10 打包前
16. prompts/cs2/brother.md 与 caster.md 仅模板路径使用（双性格已取消），
    与 [games.cs2.personality] 段的去留待定。偿还：下次动 CS2 模板路径时
17. 转视角后的地板恢复盲区（推测未证实）：连续数秒剧变会顶高中位数，
    结束后 5–10 秒内门槛偏高可能吃掉小事件。偿还：M5-T7 在 explore-3a 上
    测量「转向结束后 10 秒」的判定分布；若证实，修法为剧变帧不计入地板历史窗口
18. 区域提示稀疏上限 region_sparsity_max=0.25 已进生产（T5b），仍为初始猜测，
    与评测工具中的同名阈值须保持同源。偿还：M5-A 内扫 0.15/0.25/0.40 后统一
19. zbl 0.7.1 是相对小众的新引入依赖，维护活跃度与长期可靠性未评估。
    偿还：M10 打包前
20. 视觉链路的美国区端点因服务商账号人工审核未能实测（探针在 HTTP 403 停止），
    推理开关字段名亦未能实测。审核通过后重跑探针即可继续。
    偿还：账号审核通过时
21. ~~快线超时与丢帧策略~~ 已随 M5-T7 实现（5 秒超时、超时丢帧不重试、
    并发上限 4）。已销
22. 时间线滞后常数未定：zbl 不暴露呈现时间戳，互相关在现有素材上测不出
    可信值（过渡段非干净阶跃）。已降级为按需偿还：真需要真值级对齐时录
    专门的阶跃实验（静止中只切手电 × 10 次，两分钟）；喂模型的用途不受影响
23. 输入隐私机制：动作键白名单的结构隔离已随 M5-T7.4 在生产管道实现
    （前半已偿）；打字风暴静默、输入法合成静默仍未实现。
    **偿还硬线不变：任何分发（M10）之前必须全部实现并把全键盘开关移除或锁死**
24. 中心加权 margin：准心永远在画面中心，中心块 margin 压低、边缘抬高
    （单值或梯度），提高「准心里出现小目标」的检出率。数值须用含此类事件的
    录像经 --replay 校准，不拍脑袋。偿还：M5-A 之后有合适录像时
25. 429 冷却与错误元数据（M5-T7.8，规格已出未执行）：客户端保留
    error_type/provider_code/Retry-After/X-Generation-Id；派发级冷却+指数退避
    （非重试，丢帧教义不变）。T7.7 曾因缺元数据无法归因 53 次 429。
    偿还：T7.9–7.11 之后、真机验收之前
26. 锁定单一上游的可用性代价已实测（限流时整段失明、零故障转移）。
    受限回退清单暂不建——合格名单仅一家。重开条件：第二合格上游出现
    或美国区端点落地（技术债 20）
27. 本地 OCR 辅助——探针已证可行（中文可用、耗时够、640 素材零输出），
    两遍式有真实召回增益但朴素版超预算；定位定案：**OCR 输出是"候选提示"
    不是机械事实**（WinRT 无置信度、多版本可互相矛盾；稳定 HUD 走跨帧一致性
    投票、瞬时文字保留单帧候选不被门控过滤）。原条目：
    先离线探针测命中率（T7.10），数据出来再决定接不接管线

暂不做：
- 读游戏内存、注入进程、规避反作弊（永久禁止）；AI 操控竞技网游（永久禁止）
- macOS / Linux；账号系统、云存储、自动更新；语音或视觉推理占用 GPU
- 预先创建空模块空目录占位文件；用户自定义性格提示词
- 截图作为待机话术上下文（隐私，永久禁止）
- 同时解说多个游戏（一次一个，按 [active].game 切换，托盘手动选）
- 适配动态加载与插件市场（端口按可升级形状设计，现阶段 main.py 显式装载）
- 为"AI 代玩"类 post-MVP 做端口设计（操作输出与陪玩解说不通用）
- 各游戏数据层面做不到的事项见各自 md 的「数据边界」

## 工程原则

1. 不留向后兼容：过时代码直接删。（例外：已有真实数据的存储结构变更须先确认；
   模板回退路径是产品决策不是兼容层，必须保留。）
2. 选满足当前需求的最简实现。不做预防性抽象。
3. 先跑通最小端到端版本再往上加。绝不为未完成的功能拆掉能跑的东西。
4. 关注点分离，按已存在的职责边界拆，不为设想拆。
5. 优先用成熟且在维护的库。
6. 加依赖或自己写之前，先查现有依赖能否解决并说明查过什么。
7. 难以回退的决策（数据模型、核心接口、技术栈）按长期方案定；
   **适配端口属于此类**。易替换的实现按原则 2 从简。
8. 先看成熟产品如何解决同类问题，按本项目规模裁剪。
9. 底层行为不把假设写成诊断：按 a→b→c 尝试、每步测量、够用就停。
10. 修好一处规则后，主动搜索还有哪些地方适用同一条。
11. "能做什么"的清单优先用程序从真实数据生成，不由人或 agent 撰写。
12. 「已实现」「未实现」「技术债」是**覆盖语义不是追加**。
    自查方法：只比对两个清单中以 `- ` 或数字编号开头的条目行的任务/里程碑 ID
    是否有交集；不要在全文范围统计 ID 出现次数（交叉引用不构成重复）。
