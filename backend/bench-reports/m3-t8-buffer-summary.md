# M3-T8 发言时机复测摘要

## 评测集

- 旧 40 题移除 `round_win` / `round_loss` 后：23 题。
- 新 34 题移除回合类题目后：32 题。
- 冻结 answer key 已同步移除回合类题目；`m3-t8-real-buffer-cards-only.md` 为真实录制的 cards-only 回放。

## 缓冲结果

| 数据源 | 缓冲前策略入选 | 缓冲后发言组 | 合并组 | 合并类型 |
|---|---:|---:|---:|---|
| `gsi-20260811-223119-169538.jsonl` | 42 | 42 | 0 | 无 |
| 34 个合成场景 | 52 | 52 | 0 | 无 |

窗口为固定 1.5 秒；真实录制与合成场景均未产生窗口内的多事件组。回放测得冲刷等待为：真实录制 P50/P95/最大 1.5/1.5/1.5 秒，合成场景同为 1.5/1.5/1.5 秒。

合并卡的焦点由既有 `policy.py` 优先级选出；当前复测没有合并组，因此没有可列出的类型组合。cards-only 卡片检查中每张卡只出现一个“本次焦点”。

## 延迟与 token

- 事件卡/模型部分：真实录制沿用 M3-T6.6 cards-only 基线，P95 1.070 秒、平均输入 2387.3 token；新 34 题基线 P95 1.317 秒、平均输入 2496.8 token。由于本轮没有合并卡且未调用模型，二者均无变化。
- 新增的是发言等待：统一增加最多 1.5 秒；对局/回合/程序边界立即冲刷。

## 去重

死亡类事件现在按主体阵亡锁存，并以本回合死亡代数与死亡计数跨主体切换去重。真实录制回放中未发现同主体、同回合、3 秒内的死亡类重复事件；第 1 回合与第 6 回合的重复检查结果为 0。
当前工作区这份原始 JSONL 没有重现规格所述的 1.4/1.5 秒重复对；其中可见的第 1 回合两次普通死亡相隔 11.477 秒，第 6 回合为一次白给。这里报告的是修复后扫描结果为 0，不把未重现的旧对伪报成已复现。

## 准确率

本轮未重新调用模型；事件卡事实内容未因缓冲改变，故没有可观测的准确率下降。已有基线为旧 40 题原子事实 100%、新 34 题 97.5%。

## 语音中断

跳过实测：当前环境没有安装 Windows OneCore 中文语音，`SpeechService.load()` 返回失败；因此没有用估算值替代中位/P95/最大值。
