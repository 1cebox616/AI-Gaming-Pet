# M3-T8.10：死亡前开火区间与复测

## 实现

- `DEATH_COMBAT_WINDOW_SECONDS = 3.0`：与连续事件段处于相同量级，只把临死前的
  近身交火算入，不把更早的试探性开枪归因给这次死亡。
- `RoundSituation` 只在本人数据帧累计三项确定性观测：本回合弹匣下降时刻、最近一次
  手持武器弹匣可读时刻、以及本回合确实开过火的武器集合。
- 这三项观测只由死亡焦点渲染消费；不产生 `events.py` 事件，也不会给存活玩家贴标签。
- 既有 `击杀后被补枪` 与 `白给` 仍先按原有事件类型渲染；新标签只追加，不覆盖。

## SCENE_TAGS（22 个）

`对枪胜利`、`摸烟击杀`、`击杀后被补枪`、`白给`、`残血击杀`、`白着打`、`踩火杀`、
`换枪后立刻杀`、`白着被打死`、`出烟就没了`、`一发命中`、`干净解决`、`打了半天`、
`一梭子扫死`、`大狙空枪`、`连续空枪`、`白惨了`、`烧惨了`、`血皮撑住了`、
`对枪输了`、`一枪没开就没了`、`打空了还是没打过`。

## 标签命中与真实死亡分类

统计口径：真实录制为 `gsi-20260811-223119-169538.jsonl` 的 42 张策略入选卡；
合成场景为 35 个场景中的 53 张策略入选卡；新 32 的回合结算目标已经被产品规则排除，
实际可播报目标卡为 21 张。

| 新标签 | 真实 42 卡 | 新 32 的 21 张可播报卡 | 全部合成 53 卡 |
|---|---:|---:|---:|
| 对枪输了 | 14 | 7 | 14 |
| 一枪没开就没了 | 5 | 1 | 2 |
| 打空了还是没打过 | 0 | 0 | 0 |

真实录制有 26 次**本人**阵亡（观战后的队友死亡不属于本任务主体）。逐次按新三分类：

| 分类 | 次数 |
|---|---:|
| 对枪输了 | 17 |
| 一枪没开就没了 | 6 |
| 无法判定（3 秒窗口内没有可读手持弹匣） | 3 |
| 合计 | 26 |

“一枪没开就没了”占 6/26（23.1%），未出现超过一半的异常。`打空了还是没打过`
在现有真实与合成的策略入选卡上均未覆盖；实现有正反例回归测试，但还没有可用于语料库的
录制样本，下一轮应补一条基于真实骨架的空弹匣阵亡场景。

## 对齐提示词与答案后的复测：已停止继续调参

第一次复测仍使用旧提示词和旧答案：旧 23 的 case ID 已重排，合成集又把已取消播报的
回合结算当作目标，因而不可作为当前卡的有效准确率。经产品负责人明确同意后，本次：

- 在 `reading.md` / `inference.md` 中补齐三个死亡标签和弹药四档的确定语义及必答规则；
- 新建与当前卡绑定的旧 23、新 32 answer key，不回写历史报告的旧答案；
- 新 32 从 35 个现有场景中排除 3 个只有回合结算、当前没有播报卡的场景；其余原以
  回合结算为目标的场景，改评当前实际入选的击杀、多杀或死亡卡，因此确实评满 32 张。

按固定模型、固定上游、temperature 0、seed 42、128 token、10 秒事件/完整超时重跑。
有效产物为：

- `m3-t8.10-aligned-old23-answer-keys.json`、`m3-t8.10-aligned-old23-raw.md`、
  `m3-t8.10-aligned-old23-scores.json`、`m3-t8.10-aligned-old23-scored.md`
- `m3-t8.10-aligned-new32-answer-keys.json`、`m3-t8.10-aligned-new32-raw.md`、
  `m3-t8.10-aligned-new32-scores.json`、`m3-t8.10-aligned-new32-scored.md`

| 集合 | 整句准确率 | 原子事实准确率 | 事件 P95 | 平均输入 token |
|---|---:|---:|---:|---:|
| 旧 23 | 16/23（69.6%） | 64/68（94.1%） | 1.469s | 3077.7 |
| 新 32 | 16/32（50.0%） | 104/118（88.1%） | 1.297s | 3185.8 |

与 M3-T8.8 前发布的旧集 100%、新集 97.5% 相比，原子事实分别下降 5.9 与
9.4 个百分点，均超过 3 个百分点，故按验收规则**立即停止继续调提示词或答案**。
整句下降更大，主要失败不是死亡三分类读错，而是新增“所有标签必答”后漏掉低优先级
弹药/阶段标签，以及少量越界词：旧集 7 条整句失败；新集 16 条整句失败。

前一轮未对齐产物 `m3-t8.10-old23-*` / `m3-t8.10-new32-*` 保留作审计记录，
但已被本节的 `aligned` 产物取代，不得再引用其准确率作为 M3-T8.10 结论。

## 测试

- 全套：296 passed；4 failed，均为本机未安装 OneCore 中文语音导致的既有语音测试。
- 场景合成相关测试：120 passed。
- 覆盖：开火无掉血不算对枪、无可读弹匣不推断没开枪、有开火不标一枪没开、
  捡到空枪不标打空、以及与被补/白给并存时既有标签保留。

## 偏差与未完成项

- 未改动 `events.py`、策略、线上链路或前端。经产品负责人追加授权，改动了评测提示词，
  并新增与当前事件卡绑定的答案文件；历史答案文件保持不变。
- `打空了还是没打过` 尚无真实/合成策略入选样本命中；代码和正反例测试已完成。
- 因对齐后准确率阈值触发停止，未对两组复测失败做任何补偿性修改。
